# Bitácora — CV Reformatting Automatizado

> Registro cronológico de avances, bloqueos y comunicaciones con FITS durante la ejecución.

### 3 Jul 2026
Contrato firmado. Reloj contractual sin arrancar — pendientes: templates de branding y sitio de SharePoint propio.

### 20 Jul 2026
Recibidos los 4 templates .docx definitivos (New Format, Non Template, BD Format, Worksense Format) — cargados en `02-modulo2-agente-transformacion/templates/`. También se recibió un CV de prueba (Shirley Mercado) — cargado en `02-modulo2-agente-transformacion/cvs-prueba/`. Confirmado el sitio de SharePoint propio (`Operaciones-RecursosHumanos`, carpeta `Resumes`) y recibidos Client ID / Tenant ID del Azure AD App Registration ("CV Reformatting"), guardados en `.env` local. Client Secret agregado por Santiago directo en `.env` local. Queda pendiente confirmar los permisos otorgados sobre el sitio (`Sites.Selected` vs. `Sites.ReadWrite.All`).

Revisados dos correos de Paola: uno del 18 jun 2026 (Alternativas A y B originales) y otro del 7 jul 2026 (mismo día del kickoff) con el mapeo definitivo de workflow → etapa(s) `Convert Resume - [Formato]` para 10 workflows de JazzHR. Se detectó una laguna: ningún workflow del mapeo incluye una etapa para Worksense Format (el template de J&J). Hipótesis de Santiago: para JNJ - Workflow 2024, la etapa "New Format" debe generar el template Worksense (no la etapa genérica New Format) — sin cambios en JazzHR. Se preparó mensaje para confirmar con Paola (correo y WhatsApp).

Se reutiliza la credencial Anthropic de Fase 1 (`P3oMjAzU63IfOAff`). Se verificó la conexión a SharePoint vía Microsoft Graph API: sitio, Document Library y carpeta `Resumes` accesibles, con lectura y escritura confirmadas. Credencial `SharePoint - CV Reformatting (Graph API)` creada y probada en N8N (`fits.app.n8n.cloud`) — responde correctamente. Falta construir el workflow completo de entrega (Módulo 3).

### 21 Jul 2026
Respuesta de Paola a la duda sobre Worksense Format: FITS decidió no integrarlo en JazzHR, no es un formato de uso regular. Para JNJ - Workflow 2024 quedan solo las etapas Non Template y New Format, ambas con templates genéricos — sin lógica especial por workflow. Se descarta la hipótesis de selección de template por (workflow, etapa) planteada el 20 jul.

### 28 Jul 2026
Verificación del Módulo 1 vía API de JazzHR: las etapas `Convert Resume - [Formato]` ya estaban creadas en los 10 workflows (las agregó Paola antes del kickoff), coincidiendo con el mapeo confirmado — excepto JNJ - Workflow 2024, que tenía por error una etapa `Convert Resume - Worksense` en vez de `Convert Resume - Non Template`. Confirmado que no había riesgo de candidatos activos en esa etapa (proyecto aún sin automatizar). Santiago corrigió el nombre de la etapa directamente en JazzHR (Settings → Workflows). Verificado por API tras el cambio: JNJ queda con Non Template + New Format, igual al resto. **Módulo 1 resuelto por completo.** Se copiaron al `.env` de este repo las credenciales de JazzHR (API key, usuario, password) y el N8N API key de FITS, reutilizando la integración de Fase 1.

Se planificó la arquitectura de Módulo 2 y 3: JazzHR no tiene webhooks (hace falta un poller) y N8N Cloud no puede generar `.docx` en un Code node (bloquea `zlib`), así que se decidió un microservicio Python (`docxtpl`) en Railway para extracción y render. Se completó la Fase 0: anatomía de los 4 templates, esquema de datos canónico, mapa real de `step_id` de las 13 etapas Convert Resume vía API de JazzHR, reglas de contenido por formato y diseño del log de Sheets (aún sin crear). Se anotaron y validaron con `docxtpl` los 3 templates activos (New Format, Non Template, BD Format). Se eliminó `Worksense Template.docx` del repo (formato ya descartado por Paola el 21 jul).

Se planificó la Fase 2 (microservicio de render): ubicación acordada `02-modulo2-agente-transformacion/microservicio-render/`, validación del CV contra `cv-schema.json` con `jsonschema` (no Pydantic, para mantener el schema como única fuente de verdad), autenticación por `X-API-Key` desde el arranque. Un primer plan por fases (planner) fue auditado por Opus antes de implementar, a pedido explícito de Santiago para minimizar riesgo de bugs/crashes. La auditoría, validada renderizando de verdad contra los templates reales, encontró fallas críticas no contempladas: `docxtpl` pierde texto en silencio con `&`/`<`/`>` sin `autoescape` (frecuente en el dominio farmacéutico — "J&J", "R&D"); los tags `{{ var }}` ya anotados no son compatibles con `RichText`/`Subdoc` (generan párrafo vacío sin error); `Subdoc` requiere `docxcompose`, dependencia no acordada; cachear `DocxTemplate` entre requests arriesga mezclar datos de candidatos distintos; nombres no-ASCII (ej. "José Martínez") rompen el header `Content-Disposition`; `town: null` renderiza literalmente "None" en BD Format; BD Format no tiene el estilo de lista que sí tienen los otros dos templates (crash si se descubre recién en la fase de BD). Plan reordenado y aprobado: Fase 0 (contrato de tags completo + fixtures de prueba, incluido uno adversarial con `&`/acentos/comillas, antes de tocar los `.docx`), Fase 1 (esqueleto con límites de tamaño, verificación de integridad del `.docx` de salida, manejo de errores y auth desde el primer endpoint, config de Railway), Fase 2 (re-anotación de los 3 templates juntos + smoke test de los 3), Fase 3 (bloques y render New Format/Non Template), Fase 4 (BD Format), Fase 5 (endurecimiento: concurrencia, documentos grandes, documentación).

**Próxima sesión: arrancar la implementación por la Fase 0 del plan revisado** (contrato de tags en `templates/TAG-CONTRACT.md`, fixtures canónicos de prueba incluyendo el CV de Shirley Mercado y un caso adversarial, y el spike acotado sobre indentación de bullets).

**Fase 0 del plan revisado completada.** Se escribió `02-modulo2-agente-transformacion/templates/TAG-CONTRACT.md`, que reemplaza la sintaxis de `MAPEO-PLACEHOLDERS.md` (marcado como superado). Se corrió un spike real con `docxtpl` (no solo análisis) contra una copia de `New Format Resume Template.docx`, con datos adversariales (`Johnson & Johnson`, `R&D`, `>8%`, `<2%`, comillas, nombre no-ASCII): confirmó que un único tag `{{r bloque }}` con `RichText` construido en Python (saltos manuales `<w:br/>`, viñeta `"• "` prefijada) + `autoescape=True` en `render()` resuelve el problema de concatenación sin pérdida de datos, y que una sangría francesa (`w:ind left=360 hanging=360`, fijada una sola vez en el párrafo del `.docx` durante la anotación) sobrevive intacta al render — sin necesidad de `docxcompose`. También se confirmó que `years_experience: null` sigue imprimiendo `"None"` literal con un tag simple (motiva la guarda `{% if %}` obligatoria) y que un loop vacío no rompe el render. Se crearon los fixtures canónicos en `02-modulo2-agente-transformacion/contrato-datos/fixtures/` (`shirley-mercado.json`, con el CV real transformado según las 4 reglas de contenido, y `adversarial.json`, diseñado para ejercitar cada bug de la auditoría) — ambos validados a mano contra `cv-schema.json` (sin `jsonschema` instalado todavía).

**Próxima sesión: Fase 1 del plan revisado** — esqueleto del microservicio en `02-modulo2-agente-transformacion/microservicio-render/` (límites de tamaño, verificación de integridad del `.docx` de salida, manejo de errores y auth `X-API-Key` desde el primer endpoint, config de Railway). Instalar `jsonschema` requiere confirmación previa (dependencia nueva).

**Fase 1 completada.** Confirmado con Santiago instalar dependencias nuevas. Framework: FastAPI + `uvicorn` (lo que había recomendado Opus en el plan), con entorno virtual propio en `microservicio-render/.venv/` (no se tocó el Python global del sistema). Se creó el esqueleto completo: `main.py` (FastAPI), `requirements.txt` (fastapi, uvicorn[standard], docxtpl, jsonschema, python-docx), `Procfile` y `runtime.txt` (Python 3.12, aunque el entorno local es 3.9.6 — Railway resuelve su propio runtime) para Railway, `.env.example` y `README.md`.

Implementado y probado con servidor real corriendo en local (no solo lectura de código):
- `GET /health` sin auth, para el healthcheck de Railway.
- `POST /render` con auth `X-API-Key` obligatoria desde el primer endpoint (falla al arrancar si `API_KEY` no está seteada).
- Límite de tamaño de body (`MAX_BODY_BYTES`, default 2 MB) vía middleware — probado con un body de 3 MB, devuelve 413.
- Validación del `cv` contra `cv-schema.json` con `jsonschema` — probado con un CV incompleto, devuelve 422 con el path del campo que falló.
- `template` inválido devuelve 400; body no-JSON devuelve 400.
- Verificación de integridad: el `.docx` generado se reabre con `python-docx` antes de responder; si falla, 500.
- `DocxTemplate` se instancia de cero en cada request (sin cachear, por el riesgo de mezcla de datos entre candidatos que había encontrado la auditoría).
- `Content-Disposition` con fallback ASCII + `filename*` RFC 2231/5987 — probado con el fixture adversarial (nombre no-ASCII "José Ramón Martínez Rodríguez"), fallback cae a "?" para los caracteres no-ASCII (aceptable: los navegadores modernos usan `filename*`, el fallback solo importa para clientes viejos).

Probado end-to-end contra los 3 templates activos (`new_format`, `non_template`, `bd_format`) con ambos fixtures de Fase 0 (Shirley Mercado y el adversarial) — los 3 devuelven 200 con un `.docx` válido. **Nota esperada:** el contenido todavía sale mal formado (listas concatenadas sin salto de línea) porque los templates en `anotados/` siguen con la sintaxis vieja — se resuelve en Fase 2 al re-anotarlos según `TAG-CONTRACT.md`. Fase 1 solo validaba la plomería del servicio (auth, límites, errores, integridad), no el contenido.

**Próxima sesión: Fase 2** — re-anotar los 3 templates activos siguiendo `TAG-CONTRACT.md` (tags `{{r bloque }}` + sangría francesa fijada en el párrafo) y correr el smoke test de los 3 juntos contra el microservicio ya construido.

**Fase 2 completada.** Se hizo backup de los 3 `.docx` de `anotados/` (estado Fase 0, tags simples) antes de tocarlos. Re-anotados por script (mismo método que Fase 0: `python-docx`, no a mano en Word) siguiendo `TAG-CONTRACT.md`:
- New Format / Non Template: `years_experience` con guarda `{% if %}`, y los 4 bloques (`education_block`, `experience_block`, `certifications_block`, `skills_block`) pasaron de `{% for %}{{ }}{% endfor %}` a un único tag `{{r bloque }}` cada uno, con sangría francesa (0.25"/-0.25") fijada en el párrafo.
- BD Format: header de localidad con guarda `{% if town %}`, y 3 bloques (`summary_skills_block`, `experience_block`, `education_certifications_block`) con el mismo tratamiento.

Se creó `microservicio-render/blocks.py` (funciones que arman cada `RichText` a partir del JSON canónico, un builder de contexto por template) y `microservicio-render/smoke_test.py`, que renderiza los 3 templates contra los 2 fixtures de Fase 0 (Shirley Mercado + adversarial) y valida: ningún tag sin resolver, ningún `"None"` literal (regresión del bug de campos null), todos los bullets y skills presentes en el texto final. **6/6 combinaciones pasaron.** Se verificó además, contra el `.docx` ya re-anotado (no solo el spike de Fase 0), que la sangría francesa y los `<w:br/>` sobreviven el render real y que `&`/`<`/`>`/comillas del fixture adversarial quedan intactos.

**Alcance de esta fase:** solo templates + smoke test aislado (`blocks.py`/`smoke_test.py` corren fuera de `main.py`). El endpoint `POST /render` del microservicio (Fase 1) todavía pasa el `cv` crudo a `docxtpl` sin construir los bloques — verá contenido concatenado hasta que Fase 3/4 conecten `blocks.py` al endpoint real.

**Pendiente para Fase 2, no bloqueante:** confirmación visual en Word/Paola de la sangría y de `summary_skills_block` en BD Format (mezcla texto corrido + lista con viñeta en un mismo párrafo, ver nota abierta en `TAG-CONTRACT.md`).

**Próxima sesión: Fase 3** — conectar `blocks.py` al endpoint `/render` del microservicio (selección de builder por `template_id`, ya resuelto en `blocks.CONTEXT_BUILDERS`) y completar el render real de New Format/Non Template.

**Fase 3 completada.** `main.py` ahora construye el contexto con `blocks.CONTEXT_BUILDERS[template_id](cv)` antes de renderizar (reemplaza el paso directo del `cv` crudo de Fase 1). Como `CONTEXT_BUILDERS` ya cubría los 3 templates desde Fase 2 (necesario para el smoke test aislado), conectar el endpoint wireó los 3 de una sola vez — no hizo falta un paso separado para BD Format. Probado con el servidor corriendo en real (HTTP, no solo llamando funciones en Python): las 6 combinaciones (3 templates x 2 fixtures de Fase 0) devuelven 200 con contenido real (bullets separados, sangría, sin tags sueltos, sin `"None"` literal) vía `POST /render`. Se re-verificaron los casos de error de Fase 1 (401 sin API key, 422 con CV inválido, `/health`) — siguen intactos.

**Lo único pendiente de BD Format no es de código:** confirmación visual de FITS/Paola sobre `summary_skills_block` (mezcla texto corrido + lista con viñeta en un mismo párrafo) y sobre la sangría en general — nota abierta en `TAG-CONTRACT.md`, no bloquea el uso del microservicio.

**Próxima sesión: Fase 5 (endurecimiento)** — concurrencia, documentos grandes, documentación final. También queda pendiente el paso de negocio: enviar un `.docx` renderizado a Paola para la confirmación visual mencionada arriba.

### 29 Jul 2026

**QA visual de los 3 templates contra el fixture Shirley Mercado, con corrección iterativa en Word.** Se generaron y abrieron en Word los `.docx` de los 3 formatos para revisión visual directa (no solo smoke test). Aparecieron varios defectos que el smoke test no detecta porque solo valida contenido, no maquetación:

- BD Format: espacio en blanco enorme entre bloques (13 párrafos vacíos heredados del template original, pensados para contenido estático) y sangría francesa aplicada también a bloques 100% bullet (primer bullet sin indentar, resto sí).
- New Format / Non Template: alineación justificada heredada en `skills_block`/`education_block` generaba espaciado irregular entre palabras al tener varias líneas en un mismo párrafo.
- Encabezados de puesto (segundo trabajo en adelante) en `experience_block` no quedaban al mismo nivel que el primero — causa raíz: todo el bloque de experiencia es un único párrafo con saltos de línea manuales (`<w:br/>`), y la indentación en Word solo puede eximir la *primera línea* de un párrafo, nunca líneas intermedias. Dos intentos de arreglarlo con tabuladores personalizados (retroceder el encabezado, luego avanzar los bullets) fracasaron o quedaron frágiles.

**Decisión de arquitectura:** en vez de seguir parchando el enfoque de "un párrafo con `RichText` + saltos manuales" (elegido en Fase 0 para evitar `docxcompose`), Santiago corrigió a mano en Word los 3 renders de ejemplo aplicando listas con viñetas nativas de Word (cada bullet y cada encabezado como párrafo propio). Se leyó ese `.docx` corregido con `python-docx` para extraer las reglas exactas (indentación, presencia/ausencia de sangría francesa) y se tradujeron a un rediseño de `blocks.py` y de los 3 templates en `templates/anotados/`:

- `blocks.py` ya no usa `RichText`: los builders devuelven listas planas (`education_items`, `experience_jobs` con `header`/`bullets`, `certifications_items`, `skills_items`, `education_certifications_items`) y los templates usan loops nativos de Jinja/docxtpl (`{% for %}...{% endfor %}`) que generan un párrafo real por cada bullet y por cada encabezado de puesto — así la indentación es uniforme sin depender de la exención de "primera línea" de Word. Como ya no se concatena texto en un único run, `autoescape=True` de docxtpl resuelve `&`/`<`/`>` sin necesidad del truco de `RichText`.
- Los párrafos de control (`{% for %}`/`{% endfor %}`) quedan vacíos tras el render; se les asignó un estilo dedicado (`CV Control Tag`) para que `blocks.strip_empty_paragraphs()` los elimine sin tocar los espaciadores en blanco legítimos del diseño original (un primer intento de limpieza "borrar todo párrafo vacío" sí los borraba por error).
- Se agregó `space_after=10pt` a bullets y encabezados de New Format/Non Template (BD Format ya traía ese espaciado por defecto a nivel de documento, por eso no se notaba ahí) y, a pedido de Santiago tras un ajuste manual más, un espacio adicional de legibilidad entre los encabezados "LICENSES, TRAININGS & CERTIFICATIONS"/"SKILLS" y su primer bullet (no en Education ni en Professional Experience).
- `main.py` y `smoke_test.py` actualizados para correr `strip_empty_paragraphs()` sobre el documento ya renderizado antes de devolverlo/validarlo.

Smoke test (6/6, 3 templates x 2 fixtures) se mantuvo en verde durante todo el proceso. **Los 3 formatos quedaron confirmados visualmente por Santiago.** Esto resuelve, del lado técnico, la nota abierta en `TAG-CONTRACT.md` sobre sangría en general — sigue pendiente únicamente la confirmación visual de FITS/Paola (paso de negocio, no bloquea el uso del microservicio).

### 30 Jul 2026

Retomada la sesión para arrancar la pieza que falta del Módulo 2: el agente de transformación AI. Un plan inicial (`planner`) se auditó con Opus antes de escribir código, mismo procedimiento que se usó para el microservicio de render — esta vez verificando contra el repo real, incluida una extracción real del PDF de Shirley Mercado con `pypdf`/`pdfplumber`/`pdftotext` para comparar el orden de lectura.

**Bug encontrado y corregido de inmediato (independiente del agente, defecto ya en producción):** el header de años de experiencia renderizaba `"4++ YRS. OF EXP."` (doble `+`: el `.docx` ya traía un `+` literal después del tag, y `cv-schema.json` documentaba el campo con el `+` ya incluido) y `"+ YRS. OF EXP."` huérfano cuando `years_experience` era `null`. Ninguno de los dos casos lo atrapaba `smoke_test.py`, y pasó la confirmación visual del 29 jul sin ser notado. Corregido: los `.docx` de New Format y Non Template ahora envuelven el texto literal completo dentro del `{% if %}` (mismo patrón que `town` en BD Format), `cv-schema.json` y el fixture de Shirley se ajustaron para que el campo vaya sin `+` propio, y `smoke_test.py` gana dos asserts nuevos para esta clase de bug. 6/6 sigue en verde, verificado además con inspección directa del texto renderizado (`"4+ YRS. OF EXP."` correcto, párrafo vacío con `null`).

**Auditoría del plan del agente — hallazgos principales:** el diseño de tool use forzado necesitaba `strict: true` (no contemplado) y no debía enviar `temperature` (eliminado en los modelos Claude 5, devuelve 400); el schema derivado literal de `cv-schema.json` no sirve como tool schema sin transformar los campos `["string","null"]` a `anyOf`; el grounding check por normalización léxica, tal como estaba diseñado, marcaría `failed` al único CV real disponible (kerning del nombre en el PDF genera un espacio fantasma que separa "Merc" de "ado") y a la vez dejaría pasar invenciones reales (el propio fixture ground-truth ya tiene "food **and beverage** manufacturing", inferencia no presente en el CV original, que ningún check propuesto detectaba). Extracción de PDF: `pypdf` conserva el orden de lectura del CV real, `pdfplumber` lo desordena — se descarta instalar `pdfplumber` en v1.

**Fase 0 completada (contrato del agente, solo documentación):** `02-modulo2-agente-transformacion/agente/CONTRATO-AGENTE.md` fija los 3 estados de salida (`ok`/`review`/`failed`), el catálogo cerrado de códigos de warning (ampliado con los hallazgos de la auditoría: `COMPANY_NOT_VERIFIABLE`, `TOWN_MAY_BE_EMPLOYER_CITY`, `SECTION_COVERAGE_LOW`, `OUTPUT_TRUNCATED`, entre otros), los campos que Python sobrescribe siempre (`_meta.prompt_version`, `years_experience`), el criterio v1 de `years_experience` (con fecha de ejecución inyectable — el fixture de Shirley solo da `"4"` entre abril 2026 y marzo 2027), el modelo fijado (`claude-sonnet-5` con `strict: true`) y el default `REVIEW_BLOCKS_DELIVERY=true` (cambiado respecto al borrador original, hasta medir falsos positivos/negativos con CVs reales de FITS). Se actualizaron las referencias sueltas a "Fase 3.1/3.2/3.3" en `cv-schema.json` y `reglas-por-formato.md` (un plan anterior que nunca se escribió) para apuntar a este documento.

**Rename:** `microservicio-render/` → `microservicio/` (con `git mv`, arrastrando el `.venv` no trackeado; verificado funcional tras el cambio — smoke test 6/6). Refleja que la carpeta aloja tanto el render como el agente de transformación.

**Consecuencia sobre credenciales:** el agente corre en el microservicio Python, así que necesita `ANTHROPIC_API_KEY` como variable de entorno en Railway — no la credencial de N8N que usa Fase 1 para screening (que sigue igual, sin cambios). Mismo valor de key, dos lugares donde vive.

**Próxima sesión: Fase 1 del plan del agente** — `extract.py` (texto desde PDF/DOCX, `pypdf` como extractor primario, normalización de letter-spacing y detección `NO_TEXT_LAYER` fail-closed antes de llamar al LLM). Antes, confirmar instalación de dependencias (`anthropic`, `pypdf`, `python-dateutil`, `pytest` — ver recomendación de versiones de la auditoría) y que Santiago pegue el valor de `ANTHROPIC_API_KEY` en el `.env` local.

**Fase 1 completada.** Se instaló `pypdf==6.10.2` y `pytest==8.4.2`. Al intentar instalar se descubrió que el rename de la sesión anterior había roto el `.venv`: `activate` tenía hardcodeada la ruta absoluta vieja (`microservicio-render/.venv`), así que `pip install` caía silenciosamente al Python del sistema en vez de al entorno virtual (mensaje "Defaulting to user installation" — señal inequívoca de que no se estaba instalando en un venv real). Se recreó el `.venv` desde cero en la ruta nueva y se reinstaló `requirements.txt` completo — verificado con `sys.prefix` apuntando a la ruta correcta y smoke test 6/6 en verde. `extract.py`: detección de tipo por magic bytes (PDF/DOCX/texto plano, cubre el fallback `resumeMeta` de JazzHR), extracción con `pypdf`, fix de letter-spacing (regex validado contra los 5 headers reales que `pypdf` destroza: "PR O FIL E" → "PROFILE", etc.), saneamiento Unicode, y dos condiciones fail-closed antes de cualquier llamada a Claude: `NO_TEXT_LAYER` (CV escaneado/vacío) y `CORRUPT_FILE` (archivo dañado). 9/9 tests en `test_extract.py`, incluida una extracción real del PDF de Shirley Mercado (confirma empresas, años y los 5 headers reconstruidos).

**Fase 2 completada — pipeline completo probado con la API real de Anthropic, no solo con mocks.** `tool_schema.py` deriva el `input_schema` de la tool `emit_cv` desde `cv-schema.json` (transformador, no copia): convierte los 4 campos `type: ["string","null"]` a `anyOf` y saca `_meta.prompt_version` de `required` (Python lo sobrescribe siempre). Un spike real contra la API (modelo `claude-sonnet-5`, `strict: true`, sin `temperature`) confirmó dos cosas que la auditoría había dejado como pregunta abierta: `strict` funciona sin exigir que todos los campos estén en `required` (los opcionales como `town`/`years_experience` simplemente se omiten en vez de forzar `null`), y el resultado fue schema-válido al primer intento, con bullets bien segmentados a partir de párrafos corridos del CV original (sin bullets reales) y sin la invención "and beverage" que sí tenía el fixture viejo armado a mano.

`dates.py` implementa el criterio v1 de `years_experience` (suma de períodos sin solapamiento, `now` inyectable, meses en español e inglés, variantes de "Present"/"Actualidad", `AMBIGUOUS_DATES` para períodos con año-only) — 12/12 tests, incluido el caso real de Shirley (`"4"`, sin `+`). `agent.py` conecta extracción → prompt v1 (`prompt/transform-v1.md`, con las 4 reglas, la prohibición de inventar, matices de `town`/`period`/`skills`, y el catálogo de warnings que le corresponde emitir al LLM) → llamada con tool use forzado → post-proceso (años, `prompt_version` derivado del hash del prompt, descarte de campos desconocidos) → validación contra `cv-schema.json` con un retry de reparación si falla.

**Bug propio encontrado y corregido durante la implementación:** el primer diseño de `transform()` validaba el objeto crudo del LLM contra `cv-schema.json` **antes** de que Python insertara `_meta.prompt_version` — como ese campo es `required` en el schema completo (aunque no en el schema de la tool), la validación fallaba siempre y disparaba un retry de reparación innecesario en cada corrida. Corregido reordenando: post-proceso primero, validación después.

**Prueba end-to-end real:** `transform_cli.py` corrido contra el PDF real de Shirley Mercado (extracción → Claude → `years_experience`) en ~16-21s, y el JSON resultante renderizado contra los 3 templates reales vía `smoke_test.render()` — los 3 `.docx` salen limpios, con `"4+ YRS. OF EXP."` correcto (sin el bug del `+` duplicado corregido antes en la sesión). Es la primera vez que el pipeline completo (CV real → JSON generado por IA → .docx final) corre de punta a punta sin datos armados a mano. Se agregó `test_agent_llm.py` (marcado `@pytest.mark.llm`, opt-in vía `pytest -m llm`, no corre por defecto para no gastar tokens en cada `pytest` normal) que fija esto como regresión: valida contra el schema y compara estructuralmente contra Shirley Mercado (nunca el texto exacto del summary, que es el campo de aprobación humana).

**Próxima sesión: Fase 3** — grounding check (`grounding.py`) con la severidad reclasificada que definió la auditoría (empresas/años/métricas/I-statements como error con excepciones de dominio tipo "Phase I"; títulos/instituciones como warning; nuevo chequeo de cobertura de secciones y grounding de summary/skills/certifications) y los fixtures envenenados para probarlo sin depender de los CVs reales de FITS.

**Fase 3 completada.** Antes de escribir los tests se detectó que el propio fixture `shirley-mercado.json` seguía con el error que había encontrado la auditoría ("food **and beverage** manufacturing", invención no presente en el CV real) — se corrigió el fixture (ahora es un baseline limpio de verdad) y se migraron sus dos warnings de texto libre al formato con código del catálogo.

`grounding.py`: normalización compartida (casefold, NFKD sin diacríticos, `&`→`and`, guiones/comillas unificados) más una vista **sin espacios** específica para `full_name` — necesaria porque el PDF real de Shirley Mercado tiene un espacio fantasma por kerning tipográfico que separa "Merc" de "ado", y comparar con espacios habría marcado `failed` al propio ground truth del proyecto. Errores (bloquean): nombre, empresa (con excepción a warning si la sigla queda sin tokens verificables — J&J, 3M, BD), años de período (multiset de la fuente excluyendo dígitos de teléfonos), métricas numéricas en bullets (solo si `translated == false`; si es `true` degrada a warning `METRIC_NOT_VERIFIED`, porque la Regla 1 de traducción puede convertir "cinco" en "5"), I-statements (con lista de excepciones de dominio: "Phase I", "Operator I", etc., y `\bme\b` case-sensitive para no chocar con "ME"/Maine), y `town` inventado. Warnings nuevos: `TOWN_MAY_BE_EMPLOYER_CITY`, `TITLE_NOT_LITERAL_IN_SOURCE`/`INSTITUTION_NOT_LITERAL_IN_SOURCE` (traducciones legítimas de universidades, ej. "Universidad Politécnica" → "Polytechnic University"), `SECTION_COVERAGE_LOW` (heurística de rangos de fecha en la fuente vs. `len(experience)`), y `SUMMARY_CLAIM_NOT_VERIFIED` (grounding suave de palabras ≥7 caracteres del summary no presentes en la fuente — con una lista de vocabulario profesional genérico para no generar ruido; habría atrapado el "beverage" inventado).

13 tests en `test_grounding.py`, cada uno verificado primero a mano contra el CV real y el fixture antes de formalizarlo: empresa inventada → error; sigla corta → warning; año cambiado → error; I-statement → error; excepción "Phase I" → sin marcar; métrica inventada → error (y warning si `translated=true`); `town` inventado → error; `town` = ciudad de un empleador → warning (se encontró y corrigió un bug real acá: la comparación exigía igualdad exacta contra `location`, así que `town="Cidra"` no matcheaba `location="Cidra, PR"` — se cambió a contención); nombre con kerning simulado → sin falso positivo; empleo omitido → `SECTION_COVERAGE_LOW`. Fixture limpio → cero hallazgos.

`agent.transform()` ahora corre `grounding.evaluate()` al final: si hay errores, `TransformError("GROUNDING_FAILED", ...)` (nunca se renderiza ni se entrega); si no, los warnings se suman a `_meta.warnings` y la función devuelve `(cv, usage, state)` con `state` en `"ok"`/`"review"` según el contrato. `transform_cli.py` y `test_agent_llm.py` actualizados al nuevo contrato de 3 valores. Suite completa: 33/33 tests por defecto + 2/2 marcados `llm`. Corrida real de punta a punta contra el PDF de Shirley Mercado: `estado=review` (por `TOWN_NOT_DECLARED` y `SUMMARY_REWRITTEN`, ambos esperados), cero errores de grounding.

**Próxima sesión: Fase 4** — endpoint `POST /transform` en `main.py` (multipart/base64, mismo `X-API-Key`, límite de tamaño propio consciente de la ruta — el middleware actual es global y mataría un upload de 8MB pensado para CVs), `/transform` como `def` o cliente async para no bloquear `/render` en el mismo proceso, y ampliar `smoke_test.py` con los asserts de regresión ya usados hoy.

**Fase 4 completada.** Formato del body: JSON con base64 (`{filename, content_base64}`), no multipart — evita agregar `python-multipart` como dependencia nueva y mantiene el mismo patrón que `/render`. El límite de tamaño de body ahora es consciente de la ruta: `ROUTE_BODY_LIMITS` en `main.py` mapea `/render` → `MAX_BODY_BYTES` (2MB) y `/transform` → `MAX_TRANSFORM_BODY_BYTES` (8MB default, nueva env var). `/transform` es `async def` (mismo estilo que `/render`, parseo manual de `request.json()` para dar el mismo 400 consistente ante JSON inválido) pero la llamada bloqueante a Claude corre en `run_in_threadpool` para no trabar el event loop que también sirve `/render`. `ANTHROPIC_API_KEY` pasa a ser obligatoria al arranque (antes solo se leía del `.env`, nunca se validaba).

Al conectar `extract.py` y `agent.py` de punta a punta por primera vez en el endpoint (antes solo se probaban por separado o vía `transform_cli.py`, que tampoco los conectaba) se encontró un gap: los warnings de extracción (`SOURCE_TRUNCATED`) nunca llegaban a `cv._meta.warnings` — ni el CLI ni `transform()` los usaban. Se agregó el parámetro `extract_warnings` a `agent.transform()`, que los fusiona antes de calcular el estado `ok`/`review`. Cambio retrocompatible (default `None`) — no rompe `transform_cli.py` ni `test_agent_llm.py`.

Errores de `ExtractionError`/`TransformError` se devuelven como `422` con `{"state": "failed", "code": ..., "detail": ...}`, nunca con `cv`. Tests nuevos: `test_agent.py` (2, con un cliente Anthropic fake — primera vez que se testea `transform()` sin gastar tokens reales, usando el fixture de Shirley Mercado como respuesta simulada de la tool) y `test_main.py` (10, con `TestClient` + `monkeypatch` sobre `main.transform`, cubriendo auth, body inválido, `ExtractionError`/`TransformError` reales y simulados, y el caso clave de ruteo: un body de tamaño intermedio se rechaza en `/render` pero se acepta en `/transform`). Suite completa: 45/45 tests por defecto + 2/2 marcados `llm`.

**Próxima sesión: Fase 5** — harness de evaluación con CVs sintéticos (SLA <3min por transformación, 100% de cumplimiento de las reglas binarias del PRD), antes de avanzar a Fase 6 (wiring en N8N) y Fase 7 (ajuste fino con los 15–20 CVs reales de FITS, pendientes de que el cliente los entregue).

**Fase 5 completada.** 4 CVs sintéticos inventados a mano en `cvs-prueba/sinteticos/`, uno por cada regla de contenido del PRD (traducción, tercera persona, Objective→Summary, reescritura de summary pobre) más una combinación (español + primera persona + Objetivo). Ninguno usa datos de candidatos reales.

**Hallazgo real en la primera corrida:** 2 de los 4 CVs (los que no traían sección explícita de "Skills") fallaron con `GROUNDING_FAILED` — el modelo infirió una lista de habilidades parafraseando la experiencia (ej. "Deviation investigation" a partir de "Investigated deviations"), y el chequeo de `skills[]` en `grounding.py` exige token exacto contra la fuente, sin tolerancia a paráfrasis. Se corrigió agregando una sección de Skills/Competencias explícita a esos 2 CVs (con términos ya literales en el cuerpo), para aislar lo que cada test mide. Queda anotado como riesgo pendiente para la Fase 7 en `Decisiones.md` y `agente/CONTRATO-AGENTE.md`: si el set real de FITS trae seguido CVs sin sección de Skills, hay que decidir entre instruir al prompt a dejar `skills: []` vacío o relajar el chequeo a warning (como ya tienen `title`/`institution`).

`tests/test_synthetic_harness.py` (marcado `llm`, opt-in): un test por CV, cada uno verifica que el estado nunca sea `failed`, que el tiempo de transformación quede bajo un presupuesto interno de 60s (holgado dentro del límite de 3 minutos punta a punta del PRD, que todavía no se puede medir completo sin Fase 6), y el código de warning esperado para la regla que ese CV ejercita (`SUMMARY_FROM_OBJECTIVE`, `SUMMARY_REWRITTEN`) más chequeos de texto (sin I-statements, sin palabras en español remanentes). Corrida completa contra la API real: 4/4 en verde, tiempos entre 10s y 25s. Suite no-`llm` sigue en 45/45.

### 4 ago 2026 — Deploy a Railway y Fase 6 (poller + worker en N8N)

**Deploy a Railway:** trial gratuito (5 USD/30 días), proyecto `outstanding-nurturing` conectado al repo `Insightyai/fits-llc-cv-reformatting` vía GitHub App (aprobado por Santiago tras confirmar que su cuenta no era owner de la org). Primer intento de deploy falló: con Root Directory acotado a `microservicio/`, Railway no incluye carpetas hermanas en el build — `main.py`/`agent.py`/`tool_schema.py` dependían de `../contrato-datos` y `../templates/anotados`. Se resolvió moviendo `contrato-datos/`, `templates/anotados/` y `prompt/` **dentro** de `microservicio/` (commits `a5bd760` y `b68bf9c` — el primero solo el `git mv`, el segundo el ajuste de rutas que se había quedado sin stagear) y ajustando las rutas en 7 archivos. 45/45 tests + smoke test en verde tras el cambio. Servicio activo en `https://fits-llc-cv-reformatting-production.up.railway.app`; `/health` y `/render` verificados con curl real contra el fixture de Shirley Mercado.

**Fase 6 — Poller y Worker en `fits.app.n8n.cloud`:** se construyeron dos workflows nuevos (`JazzHR - Convert Resume Poller` id `6gxbJ87rfAsbcCOO`, `JazzHR - Convert Resume Processor` id `mp3U5XDSxLCAX9kI`), creados y verificados directamente vía la API REST de n8n (`N8N_API_KEY_FITS` en `.env`), replicando el patrón ya probado del poller/worker de AI Screening (mismo repo hermano de Fase 1). Alcance de esta sesión: solo Módulo 2 (detección → descarga de CV → `/transform` → `/render`) — Módulo 3 (SharePoint, email, Sheets) queda para otra sesión, entre otras cosas porque el Sheet de log de Módulo 3 ni siquiera existe todavía.

**Hallazgo de arquitectura — tres espacios de IDs distintos en JazzHR para el mismo concepto:** el `job.workflowId` numérico que devuelve la API interna (`api.jazz.co/job`, la que usa el poller para listar jobs) no es el mismo que el `workflow_id` alfanumérico de la API pública (`workflow_20240528144743_...`, el que ya tenía documentado `conocimiento/jazzhr-stepids-convert-resume.md`). Se correlacionaron los 10 workflows cruzando, para varios jobs conocidos, ambos valores (ver actualización del 4 ago en ese archivo) — sin esto, el poller no podía saber a qué formato pertenece cada job abierto. De paso se confirmó un tercer espacio de IDs: el candidato tiene un `id` alfanumérico (`prospect_...`, el que usa la API pública) y un `prospect_int_id` puramente numérico — la API interna de descarga de CV (`api.jazz.co/prospect/{id}`) exige el numérico, no el alfanumérico.

**Bugs encontrados y corregidos durante las pruebas reales (antes de que llegara ningún candidato real a producción):**
1. `Fetch Projobs` se rate-limiteó (429) al disparar ~130 llamadas casi simultáneas contra JazzHR — se agregó `batching` (1 request/600ms) al nodo.
2. El Code node que arma el body de `/transform` leía `binary.data.data` directamente, que con el modo de almacenamiento "filesystem" de n8n no es el base64 real sino un marcador de modo (`"filesystem-v2"`) — corregido usando `this.helpers.getBinaryDataBuffer()` (mismo helper que ya usa el worker de AI Screening para DOCX).
3. El chequeo "¿Transform OK?" solo miraba `$json.state !== 'failed'`; cuando la llamada HTTP a `/transform` fallaba de plano (400/422, con `continueOnFail`), `state` quedaba `undefined` y la condición daba `true` por error, dejando pasar un `cv` vacío hacia `/render`. Corregido agregando también el chequeo `!$json.error`.

**Verificación con datos reales:** con la cookie de sesión de JazzHR pegada temporalmente por Santiago (usada solo en memoria de la sesión de terminal, nunca guardada en archivo), se confirmó que **ningún candidato está hoy sentado en ninguna de las 13 etapas de Convert Resume** (130 combinaciones job×step revisadas, 0 errores, 0 candidatos) — coherente con que las etapas se crearon recientemente (Módulo 1) y nadie las ha usado todavía. La prueba mecánica del worker se hizo entonces con un candidato real que sí tiene CV descargable (vía AI Screening) aunque no esté en esa etapa: confirmó que todo el pipeline mecánico funciona (JazzHR → n8n → Railway → Claude), y de paso reprodujo con datos reales de FITS por primera vez el riesgo ya anotado en la Fase 5 (`GROUNDING_FAILED` cuando el CV no trae Skills explícitas).

**Estado al cierre de la sesión:** ambos workflows construidos, probados y **desactivados** a pedido de Santiago — se activan cuando FITS confirme que van a empezar a mover candidatos a las etapas Convert Resume. Credenciales nuevas creadas en `fits.app.n8n.cloud`: `CV Reformatting - Railway API Key` (id `2lvRPf8pQTIkmxUH`, header auth con el `API_KEY` del microservicio).

### 6 ago 2026 — Fixes de robustez en el Convert Resume Poller/Processor

Retomada la revisión de Fase 6 (todavía desactivada) para cerrar dos riesgos detectados al comparar `JazzHR - Convert Resume Poller` contra el patrón ya probado de `JazzHR - AI Screening Poller`:

**1. Choque de scheduling entre los dos pollers.** El Convert Resume Poller corría con un trigger `Interval`, sin coordinación con el trigger del AI Screening Poller — riesgo de que ambos pollers golpearan la API de JazzHR al mismo tiempo (el mismo patrón de rate-limit 429 encontrado con `Fetch Projobs` el 4 ago). Corregido: trigger cambiado de `Interval` a `CronExpression` con ventana de 15 minutos, alineado al horario del AI Screening Poller. Nodo trigger renombrado para reflejar que dispara por cron, no por intervalo.

**2. Riesgo de OOM (mismo síntoma que el incidente de julio, pero en el workflow nuevo).** El Convert Resume Poller no tenía límite de candidatos procesados por corrida — a diferencia del AI Screening Poller, que ya resuelve esto con `MAX_PER_CYCLE` (batch processing con throttling y deduplicación con estado). Corregido: se implementó el mismo mecanismo `MAX_PER_CYCLE` en el Convert Resume Poller, replicando el patrón de AI Screening. Se midió el tiempo de ejecución real del AI Screening Poller como referencia (rango 4.5–9.5 min) para confirmar que la ventana de 15 min del cron nuevo deja margen suficiente (328+ segundos de buffer en el peor caso).

**3. Falta de manejo de errores.** Ni el Poller ni el Processor de Convert Resume tenían `ErrorWorkflow` configurado. Corregido: ambos workflows quedaron vinculados al workflow global `Error Alert Global` (mismo patrón de notificación por email que ya usa el resto de la instancia).

**4. Política de retención de datos.** Se alineó el Convert Resume Poller al mismo criterio que el AI Screening Poller: no persistir las ejecuciones exitosas (solo errores), para evitar acumulación de datos en N8N Cloud.

**Verificación de cierre:** con la API de JazzHR se revisaron de nuevo las 13 etapas Convert Resume en los 10 workflows (135 combinaciones job×step, 0 errores) — se confirma que sigue sin haber ningún candidato en esas etapas. El estado de fondo no cambió respecto al 4 ago: los fixes de esta sesión son preventivos, no una respuesta a una falla real detectada en producción. Ambos workflows **siguen desactivados**, a la espera de que el equipo de Paola empiece a usar las etapas Convert Resume.

### 11 Ago 2026 — Ajustes de maquetación por CVs canon de Paola (bullets, header de experiencia, estructura por formato)

Santiago se reunió con Paola: FITS pidió ajustes a los 3 formatos generados y envió 6 CVs canon (2 por formato) como estándar exacto a cumplir — cargados en `02-modulo2-agente-transformacion/cvs-canon/` (no versionados, tienen datos de candidatos reales). Se comparó carácter por carácter (extracción raw de los PDF, no solo visual) contra los 3 templates activos y `blocks.py`.

**Cambios implementados** (detalle técnico completo en `templates/TAG-CONTRACT.md`, decisiones de negocio en `Decisiones.md`):
- Bullet nativo de Word (Wingdings, `U+F02D`) en New Format; guion `"-"` literal en Non Template y BD.
- Header de experiencia pasa de 1 línea a 2 (empresa+período con tab a la derecha / rol debajo), agrupando roles consecutivos en la misma empresa en un solo bloque con período total — nueva función `blocks.build_experience_companies()` + `dates.combine_periods()`.
- `experience[].period` pasa a nullable en `cv-schema.json` (CVs reales sin fechas para un proyecto, ej. Edward Cruz Vega en BD, no deben forzar al agente a inventar una fecha).
- BD Format: se quitó `"Resides in {{ town }}."` (redundante) y la lista de skills dentro de `Summary of Skills` (ningún ejemplo canon la trae); títulos con dos puntos.
- New Format: `SKILLS` ahora va antes de certificaciones (antes al revés), y el título de certificaciones se corrige a `"CERTIFICATIONS & TRAININGS"` (de paso corrige un typo viejo, "LINCENSES").

**Fuera de alcance de esta ronda, con decisión explícita de no implementar:** secciones `CORE COMPETENCIES` / `TECHNICAL & PROFESSIONAL SKILLS` con subtítulos / `ADDITIONAL EXPERIENCE`, presentes en uno de los 2 ejemplos canon de New Format (Kenneth) pero no en el otro (Yanina) — se adoptó la estructura de Yanina como estándar único. También se dejó sin resolver un hallazgo más grande: los 2 ejemplos canon de Non Template tienen secciones completamente distintas entre sí y del template fijo actual, lo que sugiere que "Non Template" podría requerir secciones variables por candidato en vez de tags fijos — decisión de Santiago: no rearquitecturar sin confirmar con Paola primero.

**Verificación:** 69 tests no-`llm` en verde (TDD — tests escritos antes de `blocks.build_experience_companies()` y `dates.combine_periods()`) + smoke test 6/6 (3 templates × 2 fixtures) + 3 `.docx` de muestra renderizados a mano con texto real de Kenneth/Edgeliz/Andrea para revisión visual de Santiago en Word, sin gastar llamadas a Claude (lo que se verificaba era maquetación, no la transformación del agente).

### 11 Ago 2026 — Activación de Fase 6, revertida de inmediato (falta Módulo 3)

FITS confirmó a Santiago que ya movieron candidatos a las etapas Convert Resume. Se activaron ambos workflows vía API de n8n (mismo mecanismo usado para construirlos el 4 ago): `JazzHR - Convert Resume Poller` (`6gxbJ87rfAsbcCOO`) y `JazzHR - Convert Resume Processor` (`mp3U5XDSxLCAX9kI`), verificados antes (`active: false`) y después (`active: true`) de la activación, `errorWorkflow` en `Error Alert Global` intacto en ambos casos.

**Revertido en la misma sesión.** Al revisar el Processor antes de dejarlo correr sin supervisión, se confirmó que el workflow termina en `POST /render` → `Responder DOCX` (el `.docx` se devuelve por el webhook y no se persiste en ningún lado) — no existe ningún nodo de entrega porque Módulo 3 (SharePoint) no está construido. Si el poller disparaba un ciclo real (cron en minutos `:15/:30/:45`, ventana corta desde la activación), el `.docx` generado se habría perdido sin dejar rastro. Se desactivaron ambos workflows de nuevo de inmediato (`active: false`, verificado vía API) antes de que corriera ningún ciclo. No hay evidencia de que se haya disparado ninguna ejecución en la ventana en que estuvieron activos.

**Decisión:** no reactivar Fase 6 hasta que Módulo 3 (entrega a SharePoint) esté construido y probado — de lo contrario el motor procesa candidatos reales sin lugar donde depositar el resultado.

### 11 Ago 2026 (continuación) — Módulo 3 construido, bug real de `.doc` legacy encontrado y corregido

**Módulo 3 construido e integrado al Processor** (`mp3U5XDSxLCAX9kI`), sigue desactivado junto con el Poller. Cambio de alcance pedido por FITS: correo grupal → notificación individual al reclutador asignado (detalle completo, incluidas las simplificaciones de implementación, en `../Decisiones.md`, 11 ago 2026). Se agregaron 10 nodos nuevos al Processor: `Fetch Job` → `Fetch Hiring Lead` (mismo patrón de Fase 1: `hiringLeadAccountId` → email, fallback `reclutamiento@fitspr.com`) → `Calcular Datos Entrega` → `Crear Carpeta Candidato` (subcarpeta por candidato en SharePoint, `Resumes/{Nombre} ({candidateId})/`, a pedido de Santiago) → (render sin cambios) → `Subir a SharePoint` → `Config Piloto` (`EMAIL_ENABLED: false` hardcodeado, gate manual de arranque) → `¿Email Habilitado?` → `Enviar Notificación` (solo si piloto activado) → `Calcular Resultado Final` → `Registrar en Sheet` → `Responder OK` (JSON, ya no binario — de paso corrige un bug latente: el poller siempre registraba `ok:false` porque la respuesta binaria original nunca traía `success`/`ok`).

**Sheet de log creado** vía workflow temporal en n8n (credencial `vuAQyDYC5NJboIEW`), 21 columnas (las 20 de `log-sheets-diseno.md` + `recruiterEmail`). ID `1EM7GeQ7AePoMyngzDsRgMgnjj85K_pIoqfa1u4ukAo4`, pestaña `Sheet1` (quedó con el nombre por defecto, cosmético). El primer intento de escritura de encabezados falló dos veces por referencias incorrectas al `sheetName` (`gid=0` en vez del gid real `594037199`) y por usar `mappingMode: defineBelow` con claves arbitrarias en vez de `autoMapInputData` — ambos corregidos y verificados leyendo la hoja de vuelta antes de seguir. Los 4 workflows temporales usados para crear/limpiar/verificar el Sheet se eliminaron al terminar.

**Prueba real antes de reactivar:** Santiago identificó un candidato real ya sentado en una etapa Convert Resume genuina (Baxter Rains, `candidateId 408862017`, job "Project Manager - IT Projects" `jobId 10936366`, etapa "9. CONVERT RESUME-NEW FORMAT"). Se confirmó el `workflowId` interno del job (`655764`, "SOW Workflow 2024") vía un workflow temporal antes de asumir el `stepId` (`10727655`) solo por el número visible en la UI. Se activó el Processor solo (el Poller siguió desactivado) y se disparó el webhook a mano con este candidato real.

**Resultado: la mecánica del webhook respondió correctamente (422, `Responder Transform Fallido`), pero la transformación falló con `GROUNDING_FAILED` antes de llegar a ejercitar ningún nodo nuevo de Módulo 3.** El chequeo de grounding marcó como error una empresa inventada (`Zephyrus Corporation`), dos rangos de años no verificables, y varias skills no verificables (`SOX`, `MS Suite`, `Agile/Scrum`, `Zoom`) — mucho más grave que el riesgo ya conocido de Fase 5 (paráfrasis de skills).

**Causa raíz encontrada (no es alucinación del modelo con CVs largos, es un bug de extracción):** el CV real de este candidato es un `.doc` legacy (binario, formato Word pre-2007 — confirmado descargando el archivo real vía un workflow temporal y verificando la firma OLE2/CFB, `\xd0\xcf\x11\xe0...`). `extract.py` (`_sniff_kind`) solo reconocía `%PDF-` (PDF) y `PK` (docx/zip) — cualquier otro formato caía al fallback `else: kind = "text"` y decodificaba los bytes binarios crudos con `utf-8, errors="replace"`, produciendo texto parcialmente decodificable que Claude recibió como si fuera el CV real. El diseño "fail-closed" (`NO_TEXT_LAYER`/`CORRUPT_FILE`) nunca contempló formatos legacy como `.doc` — solo cubría PDF corrupto o DOCX corrupto. El chequeo de `grounding.py` sí cumplió su función (bloqueó la entrega), pero el mensaje de error (`GROUNDING_FAILED`) escondía la causa real.

**Corregido con TDD:** `_sniff_kind` ahora detecta la firma OLE2/CFB (`\xd0\xcf\x11\xe0\xa1\xb1\x1a\xe1`) y `extract_text` la rechaza de inmediato como `CORRUPT_FILE` ("formato .doc legacy no soportado, se requiere PDF o DOCX"), sin llegar nunca al fallback de texto plano. Test nuevo (`test_legacy_doc_rejected`) con la firma real. 62/62 tests en verde (61 previos + 1 nuevo). Verificado además contra el archivo real descargado de Baxter Rains: ahora rechaza correctamente con `CORRUPT_FILE` en vez de alucinar. **Pendiente:** desplegar este fix a Railway (commit + push) y decidir si vale la pena avisarle a FITS que los CVs en `.doc` viejo no son compatibles (dato de negocio, no bloquea el fix técnico).

**Fix desplegado a Railway** (commit `449badc`, push inmediato tras confirmar el bug) y verificado en producción con el mismo archivo real de Baxter Rains: `/transform` ahora responde `CORRUPT_FILE` limpio en vez de alucinar.

**Segunda prueba real — Steven Palmer-Velazquez** (`candidateId 220234251`, job "Laboratory Validation Specialist" `jobId 10956327`, `workflowId 653470` = JNJ - Workflow 2024, `stepId 10727653`, New Format, etapa real "10. CONVERT RESUME-NEW FORMAT"). Primer intento también bloqueado por grounding, pero por un hallazgo distinto y mucho más leve: `GROUNDING_I_STATEMENT` disparado por "Chemistry Laboratory **I** & II" (numeración romana de un curso, no un pronombre) — la lista de excepciones de `_check_i_statement` (`phase`, `level`, `class`, `type`, `operator`, `part` antes de la "I") no cubría "laboratory", y agregarla directamente hubiera sido riesgoso (es una palabra común que sí puede preceder una "I" real: "Managed the laboratory. I also..."). Corregido con un chequeo dirigido a lo que **sigue** a la "I" (patrón `& II`/`, II`, mismo estilo que la excepción ya existente para "I/O"), no a lo que la precede — evita crear falsos negativos. Test nuevo con el caso real más un test adversarial confirmando que "laboratory" seguido de una "I" statement real sigue bloqueando. 64/64 tests en verde. Desplegado (commit `7e64660`) y verificado.

**Reintento de Steven Palmer-Velazquez tras el segundo fix: éxito de punta a punta.** `POST /transform` y `POST /render` pasaron, `Subir a SharePoint` creó la subcarpeta `Steven Palmer-Velazquez, M.S. (220234251)/` dentro de `Resumes/` y subió `New Format.docx`, `Fetch Job`→`Fetch Hiring Lead` resolvió el reclutador real (`paola@fitspr.com`), el modo piloto retuvo la notificación correctamente (`emailSent: false`, `resultado: OK_PILOTO`), y la fila se registró completa en el Sheet (verificado leyendo la hoja de vuelta) — duración real 52s, dentro del presupuesto. `Responder OK` devolvió `{success:true, sharepointUrl, emailSent, resultado}`, corrigiendo también el bug latente de logging del poller.

**Estado al cierre (antes del hallazgo de layout, ver más abajo):** Módulo 3 validado de punta a punta con un candidato real. Poller y Processor quedan **desactivados** — pendiente decisión de Santiago sobre reactivar ya (con modo piloto, `EMAIL_ENABLED: false`) para que sigan llegando candidatos reales a SharePoint mientras se revisan 3-5 antes de habilitar el email, o seguir con pruebas manuales candidato por candidato.

### 11 Ago 2026 (continuación) — Dos bugs de layout reales encontrados por Santiago revisando el .docx de Steven vs. canon de Yanina

Santiago abrió el `.docx` real generado para Steven Palmer-Velazquez (subido a SharePoint en la prueba anterior) y lo comparó directamente contra `cvs-canon/Yanina M. Castro Delgado.pdf` — no coincidía en forma, sobre todo en el header.

**Bug 1 — nombre y años de experiencia en dos líneas, no una.** El canon muestra `"YANINA M. CASTRO DELGADO"` y `"6+ YRS. OF EXP."` en la misma línea (nombre izquierda, años a la derecha). La plantilla activa (`New Format Resume Template.docx`, y también `Non Template Resume.docx`) tenía `{{ full_name }}` y el tag de años en dos párrafos separados, dentro de una sección de **2 columnas** (`w:cols w:num="2"`) — diseño roto desde el origen: Word solo reparte contenido entre columnas cuando la columna 1 desborda por altura, nunca por párrafo, así que ambos textos siempre caían apilados en la columna 1, dejando la columna 2 vacía. Nadie lo había notado porque las revisiones visuales anteriores (29 jul, 11 ago) se enfocaron en bullets/experiencia/skills, nunca en esta línea específica.

**Bug 2 — Educación sin negrita, con fecha, con viñeta — ninguna de las 3 cosas aparece en el canon.** `format_education_item()` en `blocks.py` armaba un string único `"grado, institución (período)"` en una viñeta Wingdings nativa. Los 6 CVs canon (verificado en Yanina y Andrea) muestran el grado en **negrita** en su propia línea, la institución **sin negrita** debajo, **sin fecha** y **sin viñeta**.

**Corregido:** header fusionado en un solo párrafo con tab stop derecho en `9360` (mismo valor ya usado y confirmado en `company.header`), eliminando la sección de 2 columnas — aplicado a New Format y Non Template (mismo bug en ambos). Nueva `build_education_entries()` en `blocks.py` devuelve `{degree, institution}` sin período; ambos templates ahora renderizan 2 párrafos por item de educación (negrita/plano), sin viñeta. BD Format no se tocó — su bloque de educación es estructuralmente distinto (combina educación+certificaciones en una lista, fuera de alcance de este fix). 2 tests nuevos en `test_blocks.py`. 66/66 tests + smoke test 6/6 en verde. Desplegado a Railway (commit `93bf096`) y **verificado con el mismo candidato real** (Steven, tercera corrida) — se descargó el `.docx` regenerado desde SharePoint (mismo archivo, sobrescrito en el mismo lugar) y se confirmó la estructura corregida: header en una línea, educación con negrita/sin fecha/sin viñeta.

**Confirmación visual de Santiago:** tras revisar el archivo real (sin necesidad de convertir a PDF), reportó que la tipografía seguía sin coincidir con el canon y que los bullets de `PROFESSIONAL EXPERIENCE` no se veían — screenshots de Kenneth Otero Rivera (canon) vs. Steven confirmaron ambos problemas.

**Bug 3 — fuente por defecto incorrecta (`docDefaults` en `styles.xml`).** New Format y Non Template tenían `Times New Roman` como fuente por defecto del documento (BD Format ya tenía `Arial` correctamente). Los tags dinámicos (`{{ summary }}`, `{{ company.header }}`, `{{ role.header }}`, `{{ b }}`, `{{ item }}` de skills/certificaciones, y los nuevos `{{ item.degree }}`/`{{ item.institution }}` de hoy) nunca tuvieron fuente explícita — solo los títulos de sección estáticos (EDUCATION, SUMMARY OF QUALIFICATIONS, etc.) la tenían. Resultado: casi todo el contenido real del CV salía en Times New Roman en vez de Arial, sin que nadie lo hubiera notado en las revisiones visuales anteriores (29 jul, 11 ago).

**Bug 4 — bullets de New Format invisibles.** `PROFESSIONAL EXPERIENCE`/`SKILLS`/`CERTIFICATIONS & TRAININGS` en New Format usaban viñeta nativa de Word en fuente Wingdings (`numId 9001`) — si el visor no tiene esa fuente bien mapeada, el símbolo no se ve, dejando la línea en blanco. Non Template y BD Format ya usan un guion literal `"- "` sin depender de ninguna fuente especial.

**Corregido:** `docDefaults` cambiado a Arial en New Format y Non Template (fix de una sola línea con alcance global — todo el contenido dinámico hereda la fuente correcta automáticamente). Bullets de New Format cambiados de Wingdings/`numPr` a guion literal `"- "`, igual que los otros 2 formatos — más simple y sin riesgo de fuente faltante. De paso se limpió la sangría francesa que había quedado huérfana en el fix de educación de hoy (ya no hay viñeta que alinear). 66/66 tests + smoke 6/6 en verde. Desplegado (commit `33f87d3`) y verificado con Steven por cuarta vez: fuente Arial confirmada en el `docDefaults` del archivo descargado, bullets `"- "` visibles en los 3 roles de experiencia.

**Bug 5 — párrafo fantasma entre empresa y su primer cargo.** El tag `{% for role in company.roles %}` nunca tuvo el estilo `CV Control Tag` que sí tienen todos sus `{% endfor %}` hermanos — `strip_empty_paragraphs()` solo limpia párrafos con ese estilo, así que quedaba como párrafo vacío con el tab/spacing viejo (10pt) heredado de `company.header`. Bug preexistente (no introducido hoy), enmascarado antes porque todo tenía `spacing=200` por igual — se volvió visible recién al ajustar el resto del espaciado. Corregido en los 3 templates (commit `0fe33dd`).

**Método de verificación que terminó siendo clave: conversión real a PDF vía Microsoft Graph.** En vez de solo inspeccionar la estructura XML con `python-docx` (que no revela fuente por defecto heredada, viñetas invisibles por fuente faltante, ni espaciado real), se empezó a pedir `GET .../content?format=pdf` sobre el archivo real en SharePoint — la misma conversión que usan los visores web — y leer ese PDF directamente. Esto permitió detectar dos regresiones que la inspección estructural no hubiera visto:
- **Bug 6:** al copiar el estilo de viñeta de Yanina (fuente `SymbolMT`, confirmado con `pymupdf` sobre su PDF), los bullets se volvieron invisibles en la conversión de Graph — mismo problema que Wingdings antes. Revertido a Arial con el mismo carácter `"−"` (commit `4b22ca4`), que sí se confirmó visible en esa misma conversión.
- Confirmó que un "retroceso" reportado por Santiago era en realidad una vista cacheada de SharePoint/Word Online, no un bug real — la conversión a PDF bajo demanda no tiene ese problema porque siempre lee el archivo actual.

**Lección de medición:** la distancia baseline-a-baseline entre líneas de un PDF no distingue interlineado normal de espaciado real entre bloques (ambos rondan ~11.5-13pt para Arial 10pt). Hay que medir el hueco entre el borde inferior de una caja de glifo y el borde superior de la siguiente (`y0_actual - y1_anterior`, no `y0_actual - y0_anterior`) — con esa métrica, Yanina muestra un patrón simple de 2 niveles: ~0 dentro de un bloque (líneas de un mismo párrafo, bullet a bullet, degree a institución) y ~240 twips entre bloques (título→contenido, estudio→estudio, empresa→empresa), prácticamente el mismo valor en los tres casos.

**Bug 7 — fecha de subcargo repetida.** Cuando el período de un subcargo (ej. "Laboratory Instructor") coincide exactamente con el período combinado de la empresa, mostrar ambos es redundante. `build_experience_companies()` ahora compara el período formateado del rol contra el período formateado combinado de la empresa y omite el del rol si son idénticos (commit `1ef8405`).

**Cambios finales acumulados sobre New Format** (commits `93bf096` → `4b22ca4`): header nombre+años en una línea (mayúsculas), educación con negrita/institución sin fecha ni viñeta, `docDefaults` a Arial, bullets nativos con guion `"−"` y tab real (no espacio) alineados a `x=108pt`, fecha de cargo en columna derecha real (`tab pos=9360`, mismo valor que `company.header`) en vez de entre paréntesis pegada al título, fecha de subcargo omitida cuando duplica la de la empresa, 2 niveles de spacing (bloque ~240 twips / dentro-de-bloque ~0) más un 3er nivel intermedio sin referencia en el canon (subcargo a subcargo, 120 twips, pedido explícito de Santiago), interlineado en default de Word (sin multiplicador forzado). 72 tests + smoke 6/6 en verde en cada iteración. **Confirmado por Santiago tras ~10 rondas de ajuste iterativo con verificación por PDF real:** "esta versión está bien."

**Estado al cierre:** New Format queda calibrado y confirmado contra el canon de Yanina. **Non Template y BD Format solo tienen las correcciones básicas de fuente/tamaño/spacing genérico de rondas anteriores — no pasaron por esta calibración fina.** Decisión explícita de Santiago: repetir este mismo proceso caso por caso para cada formato (con sus propios CVs canon — Andrea/Aneira para Non Template, Edgeliz/Edward para BD Format) en otra sesión, no de una sola vez. Fase 6 (Poller + Processor) sigue **desactivada**, pendiente de decisión sobre reactivarla.

### 12 Ago 2026 — Calibración fina de BD Format contra canon de Edgeliz/Edward, verificada con candidata real (Ruth Sotomayor Clavell)

**Método:** mismo que New Format — medición glyph-box con `pymupdf` sobre los 2 canon de BD (`cvs-canon/BD Edgeliz N Ramos Rosario.pdf`, `BD Edward Cruz Vega.pdf`), no solo inspección visual. Detalle completo de cada bug y su fix en `templates/TAG-CONTRACT.md` (sección BD Format); acá el resumen cronológico.

**Ronda 1 — bugs encontrados por medición contra los 2 canon (antes de tocar ningún candidato real):**
- Color de texto gris (`#67696F` en `docDefaults`) en vez de negro puro — resabio del template original del cliente, nunca limpiado (New Format no lo tenía).
- `docDefaults` con `spacing after="200"` global sin overrides por párrafo — de más en cada bullet/rol que el canon no muestra. Reemplazado por el mismo modelo de 2 niveles de New Format (0 dentro de bloque / 240 twips entre bloques), sin el 3er nivel intermedio de New Format (el canon de Edgeliz muestra rol a rol completamente apretado).
- Summary y bullets sin `jc="both"` (heredaban izquierda; el canon está justificado).
- `role.header` sin indent (el canon lo indenta a x=90pt, alineado con el guion de los bullets).
- Educación/certificaciones mezcladas en una sola lista con viñeta y fecha (`education_certifications_items`, el formato viejo) — el canon separa grado en negrita/institución sin negrita (sin fecha, sin viñeta) de las certificaciones (cada una su propio bloque, no apretadas). `blocks.build_bd_format_context()` ahora expone `education_items` (reusa `build_education_entries()` de New Format) y `certifications_items` por separado.
- **Contradicción real entre los 2 canon** sobre negrita/indent de empresa y rol (Edgeliz: ambos en negrita, rol indentado con fecha entre paréntesis; Edward: ninguno en negrita ni indentado, fechas de sub-rol sueltas dentro del bullet). Decisión de Santiago (vía pregunta directa): adoptar el patrón de Edgeliz — nuevo parámetro `role_period_style` en `build_experience_companies()` (`"tab"` para New Format/Non Template, `"parens"` para BD).
- 74 tests + smoke 6/6 en verde. Verificación visual con QuickLook (sustituye la fuente por serif — problema del visor, no del archivo) contra Shirley Mercado y datos sintéticos calcados de Edgeliz.

**Verificación con candidata real, sin usar N8N:** a pedido explícito de Santiago, se buscó un candidato real en la etapa "8. CONVERT RESUME-BD FORMAT" consultando `api.jazz.co` directamente con la cookie de sesión de Santiago (`Authorization: Bearer <JWT>`, nunca guardada en archivo) — mismo patrón de autenticación ya usado en sesiones previas, pero esta vez sin pasar por ningún workflow de n8n. Se confirmó el único job con esa etapa (`Becton Dickinson - Workflow 2024`, `workflowId 653158`, `stepId 10727630`) y se escaneó `GET /job` (194 páginas, 4850 jobs totales, sin filtro server-side disponible) buscando coincidencias — encontró 4 antes de que Santiago mismo ubicara un candidato real en la UI de JazzHR: **Ruth M. Sotomayor Clavell** (`candidateId 387627366`, job "Buyer" `jobId 10956256`, etapa real confirmada en la UI).

Pipeline completo corrido en local, llamando el código Python directamente (`extract.py` → `agent.py`, con una llamada real a Claude, → `blocks.py` → render) — sin desplegar nada ni tocar N8N: `state=review`, sin errores de grounding. El CV descargado vía `api.jazz.co/prospect/{id}/resume` (`downloadUrl` proxy a S3).

**Bug real encontrado por esta candidata (ningún canon lo cubre): `town` ausente.** El diseño original (`{% if town %}{{ town }}{% endif %}` dentro de un solo párrafo con `spacing before=240`) deja ese párrafo vacío pero con su spacing intacto, sumado al de "Summary of Skills:" — dos huecos en vez de uno. Corregido con el mismo mecanismo que ya usan los loops: `{% if %}`/`{% endif %}` como párrafos `CV Control Tag` propios, separados del párrafo de `{{ town }}` — así Jinja borra el `<w:p>` completo cuando la condición es falsa, no solo el texto (igual que un loop con 0 iteraciones no deja párrafos huérfanos).

**Ronda 2 — bugs encontrados por Santiago en el PDF real** (exportado a PDF desde Word por Santiago, no QuickLook — primera vez que se ve la fuente real, Arial, confirmada correcta):
- **Nombre y localidad sin mayúsculas.** El canon muestra `"EDGELIZ N. RAMOS ROSARIO"` / `"MAYAGÜEZ, PR"` en mayúsculas; la plantilla pasaba `{{ full_name }}`/`{{ town }}` tal cual los devuelve el agente (título/mixto). New Format ya había resuelto esto con el filtro Jinja `{{ full_name|upper }}` — mismo fix aplicado acá a ambos tags.
- **Sangría de bullets inconsistente entre la primera línea y las envueltas.** Medido con `pymupdf` sobre el PDF real de Ruth: en el canon, el guion vive solo en x=90pt y el texto arranca en x=108pt **desde la primera línea** (vía tab real, mismo patrón que ya usa `company.header`). La plantilla tenía `"- {{ b }}"` (guion + espacio literal) — el hanging indent (`ind left=720 hanging=360`) solo afecta el wrap automático de Word, no la posición del texto pegado al guion en la primera línea, así que esa primera línea quedaba en ~90-98pt en vez de 108pt (visible solo en bullets de 2+ líneas). Corregido con `<w:tab/>` real entre el guion y `{{ b }}` más un tab stop explícito en 720 twips.
- 74 tests + smoke 6/6 en verde tras ambos fixes. Re-renderizado para Ruth reusando los datos ya extraídos (sin gastar una segunda llamada real a Claude) — pendiente que Santiago vuelva a exportar a PDF y confirme.

**CV de Ruth agregado como fixture de prueba real**, mismo criterio que Shirley Mercado: `cvs-prueba/Resume- Ruth Sotomayor Clavell.pdf` (original) + `cvs-prueba/Ruth Sotomayor Clavell - BD Format (render de prueba).docx` (salida del pipeline, no canon) — versionados en git (a diferencia de `cvs-canon/`, que está en `.gitignore`). Archivos intermedios (JSON del transform, PDFs de prueba con QuickLook) se generaron y borraron del scratchpad de la sesión, nunca llegaron al repo.

**Estado al cierre:** cambios sin commitear todavía (a la espera de la confirmación de Santiago sobre la segunda ronda de fixes). Archivos modificados: `blocks.py`, `tests/test_blocks.py`, `templates/anotados/BD - Resume Template.docx`, `templates/TAG-CONTRACT.md`, `cvs-prueba/README.md` + 2 fixtures nuevos. Pendiente: confirmación visual de Santiago, luego verificación real vía Microsoft Graph (no hecha todavía para BD, a diferencia de New Format) antes de dar la calibración por cerrada. Non Template sigue sin calibración fina (Andrea/Aneira, otra sesión).

### 14 Ago 2026 — Calibración fina de Non Template (Andrea/Aneira), verificación con 2 candidatos reales (Yajaira, Luis Antonio) y bug real de grounding corregido

**Calibración de Non Template contra canon de Andrea I. Arzola Torres y Aneira Palmer Berrios** — mismo método de medición glyph-box con `pymupdf` ya usado en BD Format. El hallazgo principal: el template **no tenía ningún `w:before` explícito en ningún párrafo** (solo `w:after="0"` suelto en 7 párrafos, sin `pPrDefault`/estilo `Normal` propio) — título de sección pegado al bloque anterior, contenido pegado al título, empresas pegadas entre sí en `PROFESSIONAL EXPERIENCE`, sin ningún respiro, contra los ~240 twips de gap consistentes en ambos canon. Corregido con el mismo modelo de 2 niveles ya calibrado en New Format/BD. Además: bullets sin sangría colgante real (mismo bug ya corregido en BD — guion + espacio literal en vez de tab real con `ind left=720 hanging=360`), bullets de experiencia sin justificar (`jc="both"`, confirmado por los 2 canon con líneas envueltas llegando al mismo margen derecho), `role.header` sin tab stop (sin impacto visible en estos 2 canon, agregado por paridad), y el header con años de experiencia — decisión explícita de Santiago de quitarlo, ya que ningún canon de Non Template lo muestra (a diferencia de New Format/Yanina). 74 tests + smoke 6/6 en verde. Detalle completo en `templates/TAG-CONTRACT.md`.

**Verificación con candidata real — Yajaira Ortiz Ruiz** (`candidateId 422900145`, job "Process Development Scientist -04" `jobId 10957423`, etapa real "8. CONVERT RESUME-NON TEMPLATE"), encontrada por Santiago en la UI y traída vía `api.jazz.co` con cookie/JWT de sesión (nunca guardado en archivo), pipeline completo en local sin N8N. `state=review`, sin errores de grounding. 20 años de experiencia en 5 empresas, `certifications: []` — buen caso real para el loop vacío.

**Bug real encontrado con este candidato: espaciado doble cuando una sección con loop queda vacía.** `LICENSES, TRAININGS & CERTIFICATIONS` y `SKILLS` son títulos consecutivos con `before=240/after=240` cada uno. Word no colapsa `spacing before`/`after` entre párrafos adyacentes (a diferencia de CSS) — los suma. Con `certifications: []`, no queda ningún párrafo de contenido entre los 2 títulos para absorber el `after=240` del primero, así que el gap real fue `240+240=480` twips, el doble de lo normal — visible como un hueco anormal en el render. Mismo bug latente en New Format (mismo patrón en sus títulos SKILLS/CERTIFICATIONS), no disparado aún porque ningún candidato de prueba tuvo esa lista vacía — queda pendiente, fuera de alcance de hoy. Corregido en Non Template envolviendo título+loop de `certifications_items` y de `skills_items` cada uno en su propio `{% if %}`/`{% endif %}` (mismo mecanismo que el fix de `town` en BD Format) — cuando la lista está vacía, la sección entera desaparece y el gap con la vecina queda en un solo 240, nunca doble.

**Falso positivo de "interlineado inflado" (no era bug de template).** Santiago señaló que el PDF de Yajaira tenía el interlineado distinto al canon, sobre todo en el último bloque de skills. Causa real: el pipeline de producción (`main.py`) reabre el `.docx` renderizado con `python-docx` y llama `strip_empty_paragraphs()` para borrar los párrafos de control `{% for %}`/`{% endfor %}` — el script manual usado para generar el archivo de prueba se saltó ese paso, dejando esos párrafos de control vacíos (cada uno ocupando una línea completa, ~11-12pt extra por cada bullet/empresa). El template en sí nunca tuvo el bug. Regenerado replicando exactamente el flujo de `main.py`; confirmado por Santiago ("verificado, quedo bien").

**Re-verificación de New Format con candidato real — Luis Antonio García Sánchez** (`candidateId 431914011`, job "Project Engineer - Automation" `jobId 10938145`, etapa real "9. CONVERT RESUME-NEW FORMAT"). Primer intento bloqueado por `GROUNDING_FAILED` con 4 skills de una sola palabra (`JMP`, `SolidWorks`, `Matlab`, `Excel`) que sí estaban literalmente en la fuente.

**Bug real encontrado y corregido en `grounding.py` (no era problema del candidato ni de la plantilla):** `significant_tokens()`/`source_words` tokenizaban el texto fuente con `.split()` (solo espacios en blanco) — una skill pegada a una coma sin espacio ("Excel,") o dos skills pegadas entre sí sin ningún espacio ("JMP,SolidWorks", tal cual vino este CV real) quedaban dentro del mismo token y nunca matcheaban el valor limpio que devuelve el agente. Al ser skills de una sola palabra, no tenían ningún token alternativo de respaldo (a diferencia de `company`, donde basta que una de varias palabras coincida) y el chequeo las rechazaba como si no aparecieran en la fuente, bloqueando la transformación completa (`state=failed`). Corregido tokenizando con un split por regex (`[^\w]+`, no solo espacios) en `significant_tokens()`, en la construcción de `source_words`, y en `_check_summary_claims()` — separa cualquier palabra de la puntuación pegada, sea al borde o en el medio. Test de regresión con el patrón exacto del CV real en `tests/test_grounding.py`. 75 tests + smoke 6/6 en verde tras el fix. Re-corrida la transformación: `state=review`, sin errores de grounding, render completo sin `None`/tags sueltos/párrafos vacíos.

**Fixtures reales agregados** (mismo criterio que Ruth Sotomayor/Shirley Mercado): `cvs-prueba/Resume- Yajaira Ortiz Ruiz.pdf` + `Yajaira Ortiz Ruiz - Non Template (render de prueba).docx`, `cvs-prueba/Resume- Luis Antonio Garcia Sanchez.pdf` + `Luis Antonio Garcia Sanchez - New Format (render de prueba).docx`. Cookies/JWT de sesión de JazzHR y JSON intermedios del transform nunca se guardaron en archivo/repo.

**Estado al cierre:** los 3 formatos (New Format, BD Format, Non Template) quedan calibrados y verificados con al menos un candidato real cada uno. Pendiente: verificación vía Microsoft Graph de Non Template y BD (la que sí se hizo para New Format); el bug latente de espaciado doble en New Format cuando `skills`/`certifications` vienen vacíos; commit y push de esta sesión.

### 14 Ago 2026 (continuación) — Bug de spacing doble portado a New Format y verificación real vía Microsoft Graph de BD Format y Non Template

**Bug de spacing doble portado a New Format.** Mismo patrón encontrado hoy en Non Template: `SKILLS` y `CERTIFICATIONS & TRAININGS` son títulos con `before=240/after=240` cada uno — si `skills_items` viene vacío, el gap entre `PROFESSIONAL EXPERIENCE` y `CERTIFICATIONS & TRAININGS` se duplica a 480 twips. Corregido con el mismo mecanismo (`{% if %}`/`{% endif %}` como párrafos `CV Control Tag` propios envolviendo título+loop de cada sección). Verificado renderizando el fixture de Shirley Mercado con `certifications: []`: la sección desaparece por completo, cero párrafos vacíos. 75 tests + smoke 6/6 en verde.

**Verificación real vía Microsoft Graph de BD Format y Non Template** (el método que ya se había hecho para New Format, pendiente para los otros 2). Token obtenido con client credentials flow contra el App Registration existente (`AZURE_TENANT_ID`/`AZURE_CLIENT_ID`/`AZURE_CLIENT_SECRET` del `.env` raíz, ya provisionado desde el 20 jul). Subidos temporalmente a `Resumes/_verificacion-temporal/` en el drive de SharePoint (`SHAREPOINT_DRIVE_ID`) los renders de prueba ya generados con candidatos reales — `Ruth Sotomayor Clavell - BD Format` y `Yajaira Ortiz Ruiz - Non Template` — y pedida la conversión real (`GET .../content?format=pdf`), la misma que usan los visores web.

**Resultado: ambos formatos verificados correctos.** Fuentes confirmadas (`Arial-BoldMT`/`ArialMT` en ambos, `Calibri-Bold` solo en el footer — sin fallback a Times New Roman ni fuentes faltantes). Medición glyph-box confirma: BD Format con certificaciones cada una en su propio bloque (~240 twips entre cada una, tal como calibrado), bullets de experiencia con sangría colgante correcta y justificados; Non Template con `LICENSES, TRAININGS & CERTIFICATIONS` completamente ausente para Yajaira (`certifications: []`) y un gap único de ~248-278 twips entre `PROFESSIONAL EXPERIENCE` y `SKILLS` (no doble) — confirma en un PDF real de producción, no solo en la estructura XML, que el fix de spacing doble de esta sesión funciona. Carpeta temporal borrada de SharePoint al cerrar (no era entrega real, solo verificación).

**Estado al cierre:** los 3 formatos (New Format, BD Format, Non Template) quedan calibrados, verificados con candidato real en local, y verificados con conversión real vía Microsoft Graph. Sin bugs pendientes conocidos de layout. Commit y push de esta sesión pendiente.

### 14 Ago 2026 (continuación) — Choque de scheduling entre Convert Resume Poller y AI Screening Poller, corregido solo en Convert Resume Poller

Santiago señaló que, aunque ambos pollers corren "cada 15 minutos" (documentado el 6 ago), seguían pudiendo solaparse dentro de la hora. Antes de tocar nada, se consultó la API REST de n8n (`fits.app.n8n.cloud`, solo lectura) para ver la config real de ambos triggers, en vez de asumir lo documentado:

- **AI Screening Poller** (`aaA8NX20TGhVVEM1`, activo en producción): trigger tipo intervalo relativo "cada 10 minutos" (`minutesInterval: 10`), no un cron fijo. Se revisó el historial real de ejecuciones (`GET /executions`, solo lectura) y se confirmó que dispara de forma estable en los minutos **:00, :10, :20, :30, :40, :50** — duración real casi siempre <1 min, un caso de 3m13s en la muestra (bastante mejor que el rango 4.5–9.5 min estimado el 6 ago).
- **Convert Resume Poller** (`6gxbJ87rfAsbcCOO`, inactivo): cron fijo `"15,30,45 * * * *"` — 3 veces por hora. **Coincidía exactamente con AI Screening en el minuto :30, todas las horas** — el choque real que Santiago sospechaba.

**Decisión explícita de Santiago:** no tocar AI Screening Poller desde esta sesión bajo ninguna circunstancia (está activo y funcionando en producción) — el ajuste es solo sobre Convert Resume Poller. Se calculó matemáticamente el mejor offset posible manteniendo la cadencia de 15 min (4 marcas/hora contra las 6 marcas/hora de AI Screening): el máximo colchón alcanzable sin colisión exacta de minuto es 2 minutos, con offsets candidatos en `{2,3,7,8}` — se presentó también la alternativa de pasar a cadencia de 20 min (colchón de 5 min, siempre, pero baja la frecuencia de detección y cambiaría el margen ya calculado de `MAX_PER_CYCLE`). Santiago eligió mantener los 15 min con el offset `7,22,37,52`.

**Corregido vía API de n8n** (mismo mecanismo ya usado en sesiones previas para construir/activar estos workflows — `PUT /workflows/{id}` con el `.env` de `N8N_API_KEY_FITS`): el nodo `Cada 15 Minutos` de Convert Resume Poller pasó de `"15,30,45 * * * *"` a `"7,22,37,52 * * * *"` — ya no coincide con ningún minuto exacto de AI Screening (:00/:10/:20/:30/:40/:50). Verificado con un re-fetch del workflow tras la actualización (cron nuevo persistido) y confirmando que AI Screening Poller sigue exactamente igual (`active: true`, mismo trigger de intervalo, sin cambios).

**Hallazgo relacionado, no corregido (pertenece a AI Screening Poller, fuera de alcance por la decisión de arriba):** el nodo `Listar Jobs` no tiene retry configurado en ninguno de los 2 pollers — es la llamada más pesada de cada ciclo (pagina hasta 194 páginas de jobs). `Fetch Projobs` sí tiene retry en Convert Resume Poller (`retryOnFail: true, maxTries: 3, waitBetweenTries: 3000`) pero no en AI Screening Poller — asimetría que viene de cuando se copió el patrón el 4 ago. Sin acción en esta sesión (requeriría tocar AI Screening Poller); queda anotado para cuando Santiago decida abordarlo.

**Estado al cierre:** Convert Resume Poller listo para reactivar sin choque de minuto exacto con AI Screening Poller (colchón de 2 min en el peor caso, no garantía absoluta dado que AI Screening ocasionalmente corre más de 2 min). AI Screening Poller sin ningún cambio. Cambio hecho directo en n8n Cloud (no versionado en este repo — los workflows de n8n no viven en git).

### 14 Ago 2026 (continuación) — Reactivación de Convert Resume Poller + Processor

A pedido explícito de Santiago, reactivados ambos workflows (Módulo 2+3 completo, con Módulo 3/SharePoint ya construido y validado desde el 11 ago — a diferencia del intento revertido esa misma fecha, esta vez sí hay dónde entregar el resultado). Chequeo previo antes de activar (solo lectura vía API de n8n + curl a Railway):

- Microservicio en Railway: `GET /health` → `200 {"status":"ok"}`.
- `Config Piloto` en el Processor: `EMAIL_ENABLED: false` confirmado — la notificación al reclutador sigue retenida, SharePoint y el Sheet funcionan igual.
- `errorWorkflow` (`Error Alert Global`) enlazado en ambos workflows.
- `staticData` (dedup, `lastRunStart`) y credenciales (`httpHeaderAuth` en `Listar Jobs`/`Fetch Projobs`) intactos tras el cambio de cron de hoy — confirma que el `PUT` de la sesión anterior no rompió nada.
- `MAX_PER_CYCLE` presente en `Dedup y Filtrar` (límite de candidatos por corrida, mecanismo anti-OOM).

**Activados en orden** (Processor primero, para que el webhook esté listo antes de que el Poller dispare): `JazzHR - Convert Resume Processor` (`mp3U5XDSxLCAX9kI`) y `JazzHR - Convert Resume Poller` (`6gxbJ87rfAsbcCOO`), ambos `active: true` verificado. AI Screening Poller sigue `active: true` sin cambios.

**Estado al cierre:** los 3 workflows relevantes (AI Screening Poller, Convert Resume Poller, Convert Resume Processor) activos en producción. Modo piloto sigue activo (email retenido) hasta que Santiago revise varios CVs reales generados por el ciclo automático.

### 14 Ago 2026 (continuación) — Primer ciclo real en producción: 9 candidatos, 7 exitosos, segundo bug real de grounding encontrado y corregido

**Primer ciclo automático real** (18:07-18:26 UTC, poco después de la reactivación): el Poller encontró y despachó 9 candidatos reales al Processor, todos con ejecución `success` a nivel de n8n. De esos 9, **7 llegaron a SharePoint** (Aida Cruz Santoni, Alexandra Santiago, Eimylin Achang, Ernesto Colón Zayas, Kevin A Rivera Lopez, Yailyn Otero Torres, y uno más) — confirmado por Santiago viendo la carpeta real. Los otros 2 fallaron con `422 GROUNDING_FAILED` en `/transform` (correctamente bloqueados, sin subir nada a SharePoint):

- **Ruth Sotomayor Clavell** (`candidateId 387627366`, esta vez en etapa New Format, no BD como el 12 ago) — `GROUNDING_TOKEN_NOT_FOUND: skill='Competitive drive'`. Verificado contra su CV real (descargado de `api.jazz.co` con cookie fresca de Santiago): esa frase no aparece en ningún lado del documento — **rechazo legítimo**, el agente inventó/parafraseó la skill, el chequeo funcionó como debía (mismo riesgo ya documentado en la Fase 5).
- **Candidato real, etapa BD Format** (`candidateId 430323639`, archivo "Soto_Eli...") — `GROUNDING_METRIC_NOT_FOUND: '14001'` en un bullet sobre certificación ISO. Verificado contra el texto que realmente extrae `extract.py` (`pypdf`) del CV real: el original dice "facilitate the plant certification in ISO 14 001" — **espacio fantasma de kerning tipográfico en medio del número** (mismo tipo de artefacto ya documentado para nombres, el caso "Merc/ado" de Shirley Mercado en `agente/CONTRATO-AGENTE.md`). `extract_metrics()` corta el número en "14" y "001" por separado, así que el "14001" limpio del agente nunca matcheaba — **bug real, no un rechazo legítimo**.

**Corregido en `grounding.py`:** `_check_metrics()` ahora también acepta el match contra la fuente sin espacios (`despace()`, la misma función ya usada para verificar `full_name` — mismo mecanismo, mismo patrón ya probado en el proyecto). Test de regresión con el patrón exacto en `tests/test_grounding.py`. 76 tests + smoke 6/6 en verde.

**Verificación del poller en curso (sin cambios, funcionando bien):** Santiago preguntó por qué no aparecían candidatos nuevos ~1h después del primer lote. Confirmado con el `staticData` real del workflow (`lastRunStart` actualizándose cada 15 min, coincidiendo con el cron `7,22,37,52`) que el poller sigue disparando puntual — simplemente no encontró candidatos nuevos en los ciclos siguientes. `processedPairs` en el `staticData` confirma los 9 IDs ya marcados como procesados hoy (dedup funcionando).

**Hallazgo operativo importante: el dedup marca "procesado" al primer intento, sea éxito o falla.** `processedPairs` en el `staticData` del Poller ya tiene las 2 llaves de los candidatos fallidos (`387627366-10956256-10727655`, `430323639-10898257-10727630`) — el poller no los va a reintentar solo en el próximo ciclo, aunque el bug de `'14001'` ya esté corregido. Reprocesar al candidato de BD Format (una vez el fix esté desplegado en Railway) requiere intervención manual: llamar `/transform`+`/render` directo, o limpiar esa llave puntual del `staticData` vía la API de n8n.

**Deploy a Railway confirmado automático.** Santiago verificó en el dashboard de Railway: el push del commit `30d8cb6` disparó un deploy nuevo solo ("via GitHub"), `ACTIVE`/`Deployment successful` en ~1 minuto — no hace falta redespliegue manual en ningún push futuro.

**Reprocesamiento manual del candidato de BD Format, confirmado end-to-end.** Con el fix ya en producción, se disparó manualmente el webhook interno del Processor (`POST https://fits.app.n8n.cloud/webhook/jazzhr-convert-resume`, mismo payload `{candidateId, jobId, stepId, format}` + header `X-Internal-Token` que usa el nodo `Disparar Convert Resume` del Poller) para el candidato `430323639`. Respuesta: `{"success":true,"resultado":"OK_PILOTO"}`, `.docx` subido a SharePoint (`emailSent:false`, correcto en modo piloto). Confirma de punta a punta: el fix de `'14001'` funciona en producción real, y el mecanismo de reprocesamiento manual (bypaseando el dedup del Poller) es viable para estos casos.

**Estado al cierre de la sesión:** los 3 formatos calibrados y verificados (local + Microsoft Graph + candidato real); choque de scheduling entre pollers corregido; Poller + Processor activos en producción; 2 bugs reales de `grounding.py` encontrados con candidatos reales y corregidos (tokenización de skills pegadas a puntuación, métrica partida por espacio fantasma de kerning); deploy automático a Railway confirmado. Sigue pendiente: habilitar `EMAIL_ENABLED` tras revisar más CVs reales (incluido el de BD Format reprocesado hoy); set de 15–20 CVs reales de FITS para Fase 7; confirmación visual de FITS/Paola sobre los 3 formatos.

### 14 Ago 2026 (continuación) — Activación de notificaciones al reclutador, con gate de horario laboral (Lun-Vie 8am-5pm PR)

**Pedido de Santiago:** habilitar el envío real de correos a los reclutadores (`EMAIL_ENABLED`), con la regla de que nunca se envíen fuera de horario laboral (Lun-Vie 8am-5pm hora PR) — si un candidato se procesa fuera de ese horario, el correo se pospone y se envía en el próximo horario laboral (no se omite para siempre).

**Arquitectura implementada (los 3 workflows viven solo en n8n Cloud, no en este repo — cambios hechos vía API REST de n8n, mismo mecanismo ya usado en la sesión):**

1. **Pestaña nueva "Notificaciones Pendientes"** en el mismo Google Sheet de log (`1EM7GeQ7AePoMyngzDsRgMgnjj85K_pIoqfa1u4ukAo4`, `sheetId 483100955`) — cola de correos retrasados. Columnas: `candidateId`, `jobId`, `format`, `formatLabel`, `candidateName`, `recruiterEmail`, `sharepointUrl`, `queuedAt`, `sent`.
2. **`JazzHR - Convert Resume Processor`** (`mp3U5XDSxLCAX9kI`): `Config Piloto` → `EMAIL_ENABLED: true` (ya no retiene todo). Nuevo nodo `¿Horario Laboral?` (IF, 4 condiciones AND: `$now.setZone('America/Puerto_Rico').weekday` entre 1-5, `.hour` entre 8 y <17) después de `¿Email Habilitado?` — si es horario laboral, sigue a `Enviar Notificación` (como antes); si no, va a un nodo nuevo `Encolar Notificación` (Google Sheets append a la pestaña de pendientes) en vez de enviar.
3. **Workflow nuevo `JazzHR - Convert Resume Notification Queue Flusher`** (`A2fusycYaExIdZ4R`, activo): cron `5,20,35,50 8-16 * * 1-5` (offset de los pollers existentes, aunque este no compite por rate-limit de JazzHR — solo toca Sheets/SMTP). Lee la pestaña de pendientes → filtra `sent != true` (mismo patrón defensivo ya usado en `JazzHR - AI Screening Daily Digest`) → envía el correo → marca `sent: true` por `row_number`.

**Bug real de configuración encontrado y corregido en el camino:** el nodo de Google Sheets con `mappingMode: "defineBelow"` **exige** un `columns.schema` completo (una entrada por columna, con `id`/`displayName`/`type`/etc.) — dejarlo vacío (`schema: []`, lo que parecía razonable ya que `columns.value` sí tenía los datos) rompe el nodo con un error de validación. Afectaba tanto a `Encolar Notificación` (Processor) como a `Marcar Enviado en Sheet` (Flusher). Corregido con el schema completo en ambos.

**Verificado de punta a punta antes de dejarlo en producción real:** se construyeron workflows temporales (borrados al cerrar) para insertar una fila de prueba en la cola dirigida a `santiago@insightyai.com` y disparar el Flusher manualmente vía un webhook temporal (removido después). Confirmado: lectura de la fila pendiente → filtro → **correo aceptado por el SMTP** (`"accepted": ["santiago@insightyai.com"], "rejected": []`) → fila marcada `sent: true`. En el proceso se recreó la pestaña de la cola 2 veces (contaminación de datos de prueba durante el debugging del bug de `schema` — nunca llegó a afectar la pestaña real de log de candidatos).

**Estado al cierre:** `EMAIL_ENABLED: true` en producción — los reclutadores ya reciben notificación real cuando su candidato se procesa en horario laboral; fuera de horario, se encola y el Flusher la despacha en el próximo ciclo de horario laboral. Pestaña "Notificaciones Pendientes" vacía y lista (headers se crean solos en el primer uso real). Sin cambios en este repo — todo vive en `fits.app.n8n.cloud`.

### 14 Ago 2026 (continuación) — Bug real encontrado por Santiago revisando el Sheet: `Calcular Resultado Final` nunca se actualizó con la rama de horario laboral

Santiago preguntó por qué todas las filas del Sheet de log decían `resultado: OK_PILOTO` — esas eran filas de **antes** de activar `EMAIL_ENABLED` (correcto, nada raro ahí). Pero revisando el nodo `Calcular Resultado Final` para responder, se encontró un bug real más serio: ese nodo seguía leyendo el resultado de `Enviar Notificación` sin condicionar por qué rama corrió — cuando `¿Email Habilitado?` es verdadero, siempre intentaba `$('Enviar Notificación').first().json`, sin importar si el candidato cayó en la rama de "encolar" (donde `Enviar Notificación` nunca se ejecuta). Como eran las 17:38 hora PR al momento del hallazgo (**ya fuera de horario laboral**), cualquier candidato real que el poller encontrara en ese momento habría roto la ejecución en este punto — no afecta la subida a SharePoint (ya ocurrida antes), pero sí el registro en el Sheet y la respuesta final del webhook.

**Corregido:** nueva función `ranNode(name)` (try/catch sobre `$(name).first()`) para detectar de forma segura cuál de las 2 ramas corrió (`Enviar Notificación` o `Encolar Notificación`), sin asumir ninguna. Nuevo campo `emailQueued` en el registro del Sheet, y nuevo valor de `resultado`: `OK_ENCOLADO` (antes solo existían `OK`, `OK_PILOTO`, `ERROR_UPLOAD`, `ERROR_EMAIL`).

**Verificado antes de dejarlo así:** construido un workflow temporal aislado (Set nodes simulando `Subir a SharePoint`/`Config Piloto`/`Calcular Datos Entrega`/`Parse Payload`, más los nodos reales `Enviar Notificación`/`Encolar Notificación`/`Calcular Resultado Final` con el fix) — sin gastar una llamada real a Claude ni tocar un candidato real. Probadas ambas ramas vía un flag controlable en el body del webhook (`forceOutsideHours`): rama "dentro de horario" → `{emailSent:true, emailQueued:false, resultado:"OK"}`; rama "fuera de horario" → `{emailSent:false, emailQueued:true, resultado:"OK_ENCOLADO"}`. Ninguna rompe. Workflow temporal borrado, fila de prueba (`TEST-RESULTADO`) limpiada de la pestaña real de la cola.

**Lección:** al agregar una rama nueva a un workflow ya en producción, hay que revisar también los nodos **aguas abajo** que asumían una sola rama posible — no alcanza con probar la rama nueva en aislamiento, hay que rastrear todo lo que lee `$('NodeName')` de nodos que dejan de ejecutarse siempre.

### 15 Ago 2026 — Poller muerto desde el 6 ago (cookie vencida), bug real en cola de notificaciones, sesión cerrada con pendientes

**Causa raíz de "no llegaron candidatos nuevos ayer" (lo que reportó Santiago al reactivar los flujos):** el `Convert Resume Poller` no había vuelto a dispararse por su cron ni una sola vez desde el **6 de agosto 23:40 UTC** — 9 días. `saveDataSuccessExecution: "none"` en el workflow ocultaba esto (ninguna corrida exitosa quedaba en el historial de Executions, así que parecía "no correr" en vez de "correr y fallar en silencio"). Cambiado temporalmente a `"all"` para diagnosticar — **sigue en `"all"`, decidir si revertir**.

Con eso visible, la ejecución real mostró el verdadero problema: `Listar Jobs` fallaba con `401 - "The provided token is expired"` — la credencial **"JazzHR Cookie"** (`T5hQiFoIQSHIvCMj`, compartida con el AI Screening Poller) estaba vencida. Como el nodo tiene "continue on fail", cada página devolvía un item `{error}` sin `.id`, y el filtro aguas abajo los descartaba a todos silenciosamente — el workflow terminaba en `success` con 0 resultados, sin ninguna alerta.

**Renovada la cookie (Santiago) → poller corriendo de punta a punta otra vez.** Con eso se destrabó el lote real de 15-20 CVs de FITS para Fase 7: Paola había movido **16 candidatos** a etapas Convert Resume para pruebas. De esos, **12 ya confirmados** en `processedPairs` del `staticData` del Poller (9 del 14 ago + 3 detectados el 15 ago: Steven Palmer `220234251`, `431914011`, `231854187`). **Faltan 4 por aparecer** — deberían salir solos en cuanto el poller vuelva a correr sano.

**Bug real encontrado en el camino: pestaña "Notificaciones Pendientes" con el encabezado roto.** En vez de las 9 columnas esperadas (`candidateId, jobId, format, formatLabel, candidateName, recruiterEmail, sharepointUrl, queuedAt, sent`), la hoja solo tenía una columna `EMAIL_ENABLED` con valor `TRUE` — alguien escribió ahí por error (ese valor en realidad está hardcodeado en el nodo `Config Piloto` del Processor, no se lee de ningún Sheet). Rompe **todo** el sistema de cola de notificaciones fuera de horario laboral, no solo estos 2 candidatos. Confirmado con lectura directa vía API cruda de Google Sheets (`GET .../values/Notificaciones Pendientes!A1:Z5`) — **el fix (restaurar el encabezado de 9 columnas) nunca se aplicó, quedó pendiente de confirmación de Santiago.**

**Consecuencia concreta:** `431914011` y `231854187` tienen su `.docx` ya subido a SharePoint (confirmado, respuesta real de Microsoft Graph) pero **nunca quedaron logueados en el Sheet ni con notificación encolada** — el reclutador no se va a enterar. Reintentar el webhook del Processor para ambos (mismo payload `candidateId/jobId/stepId/format` + header `X-Internal-Token`) falló de nuevo por el mismo motivo (2 veces, no es un problema transitorio).

**Efecto secundario de un workflow temporal de diagnóstico:** al armar un workflow temporal sin filtro de `status` para mapear el backlog completo (loop pesado sobre todos los jobs, no solo los `Open`), coincidió con el ciclo real del poller de las 16:22 UTC y lo hizo colapsar (`crashed`, 8+ min). El Poller terminó **desactivado solo** después de ese crash — sigue en `active: false` dos días después. El workflow temporal (`TEMP - Leer Header Notificaciones Pendientes`, id `PySbefN8QzeTFxJD`) **quedó activo en producción, sin borrar** — pendiente de limpieza.

**Pendientes para la próxima sesión, en orden:**
1. Renovar la cookie de JazzHR (casi seguro vencida de nuevo, duró ~1h la última vez).
2. Reactivar el `Convert Resume Poller` (`6gxbJ87rfAsbcCOO`, hoy `active: false`).
3. Restaurar el encabezado real (9 columnas) de la pestaña "Notificaciones Pendientes", reemplazando el `EMAIL_ENABLED`/`TRUE` que quedó ahí por error.
4. Reprocesar `431914011` (job `10938145`, `non_template`) y `231854187` (job `10698792`, `new_format`) para que queden logueados y con notificación encolada.
5. Borrar el workflow temporal `TEMP - Leer Header Notificaciones Pendientes` (`PySbefN8QzeTFxJD`).
6. Decidir si revertir `saveDataSuccessExecution` a `"none"` o dejarlo en `"all"`.
7. Confirmar que aparecen los 4 candidatos restantes del lote de 16 de Paola una vez el poller esté sano.

**Lección:** un workflow de diagnóstico "solo lectura" puede tener el mismo costo de carga que uno de producción si no respeta los mismos filtros (acá, el filtro `status === 'Open'`) — evaluar el volumen real antes de correrlo contra la instancia compartida, no asumir que "es de lectura" = "es liviano".

### 17 Ago 2026 — Bug real de formato incorrecto en producción (Poller), encontrado y corregido con candidatos reales afectados

**Retomando los pendientes de la sesión del 15 ago:** encabezado de 9 columnas de "Notificaciones Pendientes" restaurado y workflow temporal de diagnóstico (`PySbefN8QzeTFxJD`) borrado. Santiago reactivó manualmente `Convert Resume Poller` y `Convert Resume Processor` en la UI de n8n — quedaron `active: true` los dos.

**Incidente aparte, no relacionado:** mientras se reactivaba, `JazzHR - AI Screening Poller` y `JazzHR - AI CV Screening` (workflows de Fase 1, proyecto hermano) se autodesactivaron por crashes repetidos. Confirmado que no tiene relación con las acciones de esta sesión (los correos de autodesactivación llegaron antes de que se tocara nada de Convert Resume, y son workflows distintos). Santiago los reactivó por su cuenta; causa raíz no investigada a fondo (Santiago descartó que sea la cookie vencida), queda fuera de alcance de este proyecto.

**Hallazgo real, grave: el Poller mandó un CV en el formato equivocado a un candidato real, con notificación real ya enviada al reclutador.** Santiago encontró que **Aida Cruz Santoni** (`candidateId 431501219`, job `10952171` "Sr. Quality Engineer") está en etapa real "9. CONVERT RESUME-NEW FORMAT" en Jazz, pero el sistema generó y entregó un `.docx` en **Non Template**, con correo real ya enviado a la reclutadora asignada (`eterron@fitspr.com`, `emailSent: true`, ejecución `#19985` del Poller, 17 ago ~16:22 UTC).

**Causa raíz confirmada con evidencia dura (comparando la ejecución real contra los datos crudos de JazzHR), no por inspección de código en abstracto:** el nodo `Extraer Pares` del Poller reconstruía el `stepId`/`format` de cada candidato por **índice** (`item.pairedItem.item`, buscando hacia atrás en la salida de `Filtrar Open y Mapear Formatos`), en vez de usar el campo `workflowStepId` que la propia API de JazzHR (`GET /job/{jobId}/projob`) ya devuelve en la misma respuesta. Para Aida, JazzHR devolvió `workflowStepId: 10727655` (New Format, coincide exactamente con lo que Santiago vio en la UI), pero el índice reconstruido apuntó al contexto de un job completamente distinto ("Project Coordinator", `stepId 10727634`, Non Template). Revisando el resto del lote de esa misma ejecución, al menos 4 de los otros 7 pares también tenían el contexto mal asignado — el bug no es un caso aislado, es sistémico y no determinístico (parece depender del comportamiento de `pairedItem` bajo el batching de `Fetch Projobs`, `batchSize:1`/`batchInterval:600`).

**Fix aplicado en caliente vía API de n8n (`PUT /workflows/{id}`, mismo mecanismo ya usado en sesiones previas), sin pausar el Poller:** `Extraer Pares` reescrito para usar `pj.workflowStepId` (dato devuelto directamente por JazzHR, verificable) contra un diccionario fijo `stepId → format`, eliminando por completo la reconstrucción por índice. Verificado con un re-fetch del workflow (el código nuevo ya no contiene `pairedItem`, sigue `active: true`). El siguiente ciclo del Poller después del fix procesó 4 candidatos reales más (Ruth M. Sotomayor Clavell, Eli Soto, Yailyn Otero Torres, Eimylin Achang) con formato correcto.

**Segundo candidato real afectado por el mismo bug, encontrado al auditar las ejecuciones previas al fix (no reportado por Santiago, encontrado proactivamente cruzando timestamps de correos enviados contra la hora del fix):** **Yailyn Otero Torres** (`candidateId 73009489`, job `10956253`). Su `workflowStepId` real según JazzHR es `10727629` (Non Template), pero la ejecución `#20007` (17 ago ~17:22 UTC, antes del fix) le generó y notificó un `.docx` en **New Format** a `paolarod@fitspr.com`. El ciclo siguiente (ya con el fix) generó y notificó correctamente el `.docx` en Non Template para la misma candidata — ambos archivos quedaron conviviendo en su carpeta de SharePoint hasta la limpieza.

**Limpieza aplicada (vía Microsoft Graph API, `DELETE` directo por path, sin tocar n8n):** borrado el `.docx` en el formato incorrecto de la carpeta de SharePoint de ambas candidatas (`New Format.docx` en la carpeta de Yailyn, `Non Template.docx` en la de Aida). Verificado con un re-listado de cada carpeta: solo queda el archivo correcto en las dos.

**Pendiente, no resuelto en esta sesión:** avisarle a `eterron@fitspr.com` (caso Aida) y a `paolarod@fitspr.com` (caso Yailyn) que el primer correo que recibieron tenía el enlace al formato equivocado, con el enlace correcto. El conector de Gmail de Santiago tiene el token OAuth vencido (mismo aviso que salió al inicio de la sesión) — no se pudo crear el draft ni enviar. El contenido del correo de corrección para Aida ya quedó redactado en el chat de esta sesión, pendiente de reautorizar Gmail y enviarlo (o hacerlo manualmente).

**Hallazgo menor, sin impacto real, separado del bug de formato:** candidato `431386437` falló en la ejecución `#20017` con `NO_RESUME_FILE` (no tiene CV cargado en su perfil de Jazz) — no se generó ni envió nada, es un problema de datos en Jazz, no del Poller/Processor.

**Aún pendiente de sesiones anteriores:** reprocesar manualmente `431914011` (Luis Antonio García Sánchez, job `10938145`) y `231854187` (Emmanuel García Arroyo, job `10698792`) — Santiago confirmó directamente en Jazz que **ambos son New Format** (no lo que decía la nota de pendientes del 15 ago, que tenía a `431914011` mal anotado como `non_template`, un error de tipeo, no un cambio real de etapa). El token del webhook interno (`X-Internal-Token`) se recuperó del nodo `Disparar Convert Resume` vía API de n8n para tenerlo a mano. Decidir si revertir `saveDataSuccessExecution` a `"none"` y confirmar los 4 candidatos restantes del lote de 16 de Paola siguen sin resolverse.

**Lección:** cuando un nodo reconstruye un dato de contexto por posición/índice en vez de por un identificador explícito que ya viene en los datos, cualquier asincronía o batching aguas arriba (acá, `Fetch Projobs` con `batchSize:1`) puede desalinearlo silenciosamente — sin ningún error, sin ningún log, con `success: true` en la respuesta. La única forma de detectarlo fue comparando la ejecución real contra la fuente de verdad (la propia respuesta de la API), no leyendo el código de la lógica en abstracto.

### 17 Ago 2026 (continuación) — Reproceso liviano de Emmanuel, dos víctimas más del bug encontradas por archivos duplicados, y link de carpeta agregado a las notificaciones

**Reproceso de `231854187` (Emmanuel García Arroyo) sin repetir transform/render/upload.** Como su `.docx` en New Format ya existía en SharePoint desde el 14-15 ago (solo faltaba el logueo y la notificación), se armó un workflow temporal (`fjyTlqQNCn8D7I50`, borrado al terminar) que replica solo la "cola final" del Processor — `Fetch Job` → `Fetch Hiring Lead` → `Obtener Candidato` (con `expand=projobs.workflowStep` para verificar el formato real contra JazzHR antes de loguear, mismo mecanismo del fix) → gate de horario laboral → `Enviar Notificación`/`Encolar Notificación` → `Registrar en Sheet`, usando las mismas credenciales ya configuradas en el Processor real. Resultado: `resultado: OK`, correo real enviado a `ncaraballo@fitspr.com`, logueado correctamente. Nota: `sourceLanguage`/`translated` quedaron vacíos en esa fila a propósito — esos datos solo los devuelve `/transform`, que no se volvió a correr.

**`431914011` (Luis Antonio García Sánchez) resultó ser un tercer caso real del bug, más grave que Emmanuel: no tiene ningún `New Format.docx` en SharePoint, solo `Non Template.docx`.** El "New Format" mencionado en la sesión del 14 ago era solo un render de prueba local (fixture, nunca subido a producción) — el archivo real que sí llegó a SharePoint fue generado por la corrida de producción de esa fecha, casi con certeza con el mismo bug de `Extraer Pares` ya corregido hoy. A diferencia de Emmanuel, este candidato **sí necesita correr el Processor completo** (no hay ningún documento correcto ya generado para reusar) — queda pendiente, no se ejecutó en esta sesión.

**Dos víctimas más del mismo bug, encontradas por Santiago al notar carpetas con 2 archivos:** **Eli Soto** (`430323639`, job `10898257`) tenía `BD Format.docx` (14 ago, `OK_PILOTO`) y `New Format.docx` (hoy, post-fix, `OK`, correo real a `paolarod@fitspr.com`). **Eimylin Achang** (`152551185`, job `10953542`) tenía `New Format.docx` (14 ago, `OK_PILOTO`) y `Non Template.docx` (hoy, post-fix, `OK`, correo real a `epadilla@fitspr.com`). Diferencia importante con Aida/Yailyn: el 14 ago el modo piloto todavía tenía el correo desactivado (`emailSent: false` en ambas filas viejas), así que **nadie llegó a recibir el formato equivocado por correo** — solo quedó el archivo viejo sin uso en SharePoint. Se confirmó cuál era el correcto contrastando contra la corrida de hoy (ya con el fix, basada en `workflowStepId` real de JazzHR) y se borró el archivo viejo de cada carpeta.

**Total de víctimas confirmadas del bug de `Extraer Pares` hasta ahora: 5** (Aida, Yailyn, Luis Antonio, Eli Soto, Eimylin) — de esas, solo Aida y Yailyn tuvieron un correo real con el formato equivocado ya enviado.

**Feature nueva: el correo al reclutador ahora incluye el link a la carpeta completa del candidato en SharePoint, además del link al documento** (pedido por Santiago — más útil para el reclutador que solo el `.docx` suelto). Cambios:
1. **Processor** (`mp3U5XDSxLCAX9kI`): nodo nuevo `Obtener URL Carpeta` (GET a Graph API sobre `root:/Resumes/{carpeta}`, mismo patrón ya usado manualmente varias veces hoy) insertado entre `Crear Carpeta Candidato` y `Preparar Body Render` — necesario porque `Crear Carpeta Candidato` solo devuelve el `webUrl` la primera vez (usa `conflictBehavior: fail`, así que en cualquier reproceso posterior falla con carpeta ya existente y no trae la URL). `Enviar Notificación` y `Encolar Notificación` actualizados para incluir ambos links.
2. **Sheet "Notificaciones Pendientes"**: columna `folderUrl` agregada (pasó de 9 a 10 columnas) — hecho con un workflow temporal que primero *lee* el encabezado real y lo valida contra lo esperado antes de escribir, para no repetir el incidente del encabezado roto de la sesión del 15 ago.
3. **`JazzHR - Convert Resume Notification Queue Flusher`** (`A2fusycYaExIdZ4R`): mismo cambio en su correo, leyendo `folderUrl` de la fila encolada.

Sin candidatos nuevos disponibles para probar el cambio de punta a punta en esta sesión (el Poller no encontró nada nuevo) — queda por confirmarse con el próximo candidato real.

**`431914011` (Luis Antonio García Sánchez) resuelto en esta misma sesión, cerrando el quinto y último caso conocido del bug.** Se disparó el Processor completo vía el webhook interno (mismo mecanismo que usa el Poller): `POST /transform` + `/render` regeneraron el `.docx` real en New Format desde cero (`promptVersion: transform-v1-14a9d2b9ad97`, `sourceLanguage: en`), subido a SharePoint, correo real enviado a `astorres@fitspr.com`, `resultado: OK`. De paso confirma en producción real el feature del link de carpeta agregado más arriba: el nodo `Obtener URL Carpeta` devolvió correctamente `https://netorg583168.sharepoint.com/sites/Operaciones-RecursosHumanos/Shared%20Documents/Resumes/Luis%20Antonio%20García%20Sánchez%20(431914011)`. El `Non Template.docx` viejo (incorrecto) se borró de su carpeta.

**Pendiente para la próxima sesión, actualizado:**
1. Avisar a `eterron@fitspr.com` (Aida) y `paolarod@fitspr.com` (Yailyn) del correo con el link equivocado — sigue bloqueado por el token OAuth de Gmail vencido.
2. Decidir si revertir `saveDataSuccessExecution` del Poller a `"none"`.
3. Confirmar los 4 candidatos restantes del lote de 16 de Paola. **Hipótesis de por qué el Poller nunca los detectó (sin confirmar):** el Poller solo mira jobs con `status === 'Open'` — hay 2,414 jobs no-abiertos (Closed/Filled/On Hold/Cancelled) sobre los mismos 10 workflows conocidos, y si alguno de los 4 quedó en un job que se cerró/llenó mientras el candidato seguía sentado en Convert Resume (plausible dado que el Poller estuvo caído 9 días, 6-15 ago), el Poller nunca lo va a ver — no sería un bug, es una limitación del filtro por diseño. **No se investigó escaneando el backlog** (sería repetir el mismo tipo de carga que crasheó la instancia el 15 ago) — la forma segura de confirmarlo es que Santiago filtre en la UI de Jazz por "Workflow Status" (cada una de las 3 etapas Convert Resume), que sí funciona across todos los jobs sin importar su status.
4. Ya verificado en producción real (caso Luis Antonio) que el link de carpeta llega bien en el correo inmediato — falta verificar el camino encolado (`Enviar Notificación (encolada)` del Flusher) con un candidato real fuera de horario laboral.
5. Última corrida del Poller antes de cerrar sesión (`#20128`, 6:22 PM hora PR): `success`, 0 candidatos nuevos.

### 18 Ago 2026 — Cookie vencida de nuevo, y los 4 candidatos faltantes del lote de Paola resultan ser 2 bugs sistémicos distintos (no el filtro de jobs Open)

**Cookie de JazzHR vencida otra vez (tercera vez documentada).** Santiago reportó que desde el procesamiento de Luis Antonio (17 ago) no se había procesado ningún candidato nuevo. Verificado vía API de n8n: el Poller corrió puntual toda la noche, pero `Listar Jobs` empezó a fallar con `401 - token expired` a las 10:52 UTC de hoy (la cookie sí venía funcionando bien horas antes). Santiago renovó la cookie manualmente; el Poller se recuperó solo en el siguiente ciclo. Confirmado que ahora Santiago recibe un correo automático de aviso cuando la cookie está por vencer (mecanismo ya armado de su lado, fuera de este repo).

**La hipótesis anotada el 17 ago ("el Poller solo mira jobs Open, puede que los 4 faltantes estén en jobs cerrados") resultó ser incorrecta.** Santiago pasó los links de JazzHR de 4 de los 6 candidatos de la lista de Paola que nunca aparecieron en el Sheet (de 16 nombres reales, no 15 — se excluyó a Edwin Santos Rodríguez de la revisión, y no llegó a tiempo la captura de William Velez Serrano). Los 3 jobs involucrados (`Project Manager - IT Projects`, `Safety & Project Coordinator`, `Process Development Scientist -04`) están **Open** — confirmado consultando la API interna `api.jazz.co` directamente con una cookie de sesión que Santiago compartió solo para esta sesión (no se guardó en ningún archivo del repo). La causa real son dos bugs distintos, ninguno relacionado con el status del job:

**Bug 1 — `Fetch Projobs` recibe 429 de `api.jazz.co` y `Extraer Pares` lo descarta en silencio.** De las 135 combinaciones job+etapa que el Poller intenta revisar por ciclo, **75 (55%) fallaron con `429 - Try spacing your requests out`** en dos ejecuciones reales distintas que se revisaron a fondo (`batchInterval` de 600ms entre llamadas, con reintento 3x/3s que tampoco alcanza a evitar el límite). `Extraer Pares` hace `if (item.json.error) continue;` sin loguear nada — así quedaron invisibles, ciclo tras ciclo, los jobs de **Baxter Mickey Rains** y **Emilio Nieves** (mismo job, `Project Manager - IT Projects`, id `10936366`) y **Yajaira Ortiz Ruiz** (`Process Development Scientist -04`, id `10957423`).

**Bug 2 — `Dedup y Filtrar` marca `processedPairs` antes de confirmar que el Processor entregó algo.** **Luis A. Moreno González** (`431386437`, job `10945444`) sí fue detectado el 14 de agosto y quedó marcado como procesado ese mismo día en el `staticData` del Poller — pero el Processor nunca llegó a generar nada: `Listar Documentos` (`GET /prospect/{id}/documentCollection`) devolvía vacío para él, así que `Encontrar CV` concluía "sin archivo" y el flujo respondía `Responder Sin CV` sin tocar `/transform`. Como el dedup ya lo había marcado como hecho, quedó atascado desde el 14 sin ningún reintento ni alerta — se descubrió recién hoy cruzando manualmente `staticData.processedPairs` contra el Sheet real.

**Causa raíz del "sin archivo" de Luis Moreno, encontrada y corregida en producción.** Consultando `api.jazz.co/prospect/431386437` directamente: su currículum no está en `documentCollection` (array vacío) sino enlazado directo en el registro del candidato vía `resumeId` — probablemente porque aplicó vía "LinkedIn Apply Connect" en vez del formulario estándar (`applyContext: linkedin-ac`). Se encontró el endpoint de respaldo (`GET /prospect/{id}/resume` → devuelve `downloadUrl`, que sí sirve el binario real) y se agregó como fallback al Processor (`mp3U5XDSxLCAX9kI`): nodos nuevos `¿Usar Fallback Resume?` (IF), `Obtener URL Resume` y `Descargar CV Fallback`, insertados entre `¿Tiene Archivo?` y `Descargar CV` sin tocar el camino normal. Verificado de punta a punta con Luis Moreno real: el fallback encontró y descargó su CV correctamente — el reproceso terminó bloqueado por una razón distinta y legítima (`GROUNDING_TOKEN_NOT_FOUND: 'Confined Space Rescue Certification'`, verificado que esa frase no aparece en ningún lado del PDF real — la IA la inventó, mismo patrón que Ruth Sotomayor el 14 ago, el chequeo funcionó como debía).

**Resultado de los 4 casos investigados hoy:**
- **Emilio Nieves** (`431957584`) — reprocesado manualmente vía webhook interno del Processor, `OK`, correo real enviado.
- **Yajaira Ortiz Ruiz** (`422900145`) — mismo mecanismo, `OK`, correo real enviado.
- **Luis A. Moreno González** (`431386437`) — fix de fallback confirmado funcionando, pero bloqueado por rechazo legítimo de grounding (certificación inventada). Requiere revisión manual del CV, no se puede forzar.
- **Baxter Mickey Rains** (`408862017`) — bloqueado por un motivo distinto y real: su archivo en JazzHR es un `.doc` legacy (binario, pre-2007), rechazado a propósito por `extract.py` (`CORRUPT_FILE`). Necesita que suban una versión en PDF/DOCX.

**Nota metodológica:** la API pública de JazzHR (`api.resumatorapi.com`) no soporta búsqueda por nombre ni filtros por `job_id`/`status` (los parámetros se ignoran en silencio, siempre devuelve los últimos 100 registros). Toda la investigación de candidatos específicos por nombre se tuvo que hacer con Santiago revisando la UI de JazzHR directamente y pasando los links — no hay atajo por API para eso.

**Pendiente para la próxima sesión — plan de fix para los 2 bugs sistémicos, con verificación por paso:**

*Bug 1 — 429 silencioso en `Fetch Projobs`:*
1. Subir `batchInterval` (hoy 600ms) a un valor más seguro → verificación: correr el Poller y confirmar que los errores de `Fetch Projobs` bajan de 75/135 a ~0 en un ciclo real.
2. `Extraer Pares` deja de descartar los items con `.error` en silencio — acumularlos y dejarlos visibles (pestaña de Sheet o campo en `staticData`) → verificación: forzar o esperar un 429 real y confirmar que queda registrado, no que desaparece.

*Bug 2 — `processedPairs` marcado antes de confirmar éxito:*
3. Mover `staticData.processedPairs[key] = today` de `Dedup y Filtrar` a después de la respuesta de `Disparar Convert Resume`, distinguiendo: éxito → marcar normal; falla permanente conocida (`CORRUPT_FILE`, `GROUNDING_FAILED`) → marcar con el motivo, visible; falla transitoria/desconocida (timeout, 5xx) → no marcar, dejar que el siguiente ciclo reintente → verificación: simular las 3 rutas y confirmar que solo las dos primeras quedan marcadas.
4. Registrar las fallas permanentes en una pestaña nueva del Sheet ("Errores de Procesamiento") con candidateId + motivo → verificación: forzar una falla conocida y ver la fila en el Sheet.

*Limpieza retroactiva:*
5. Auditar las claves actuales de `processedPairs` contra el Sheet real (mismo método usado hoy) para encontrar otros candidatos "fantasma" además de Luis Moreno → verificación: lista cruzada, cada caso reprocesado o documentado con motivo.
6. Revisar a William Velez Serrano, pendiente de esta sesión (nunca llegó su captura) → verificación: confirmar su job/status y reprocesar si corresponde.

### 18 Ago 2026 (continuación) — Bug 1 investigado a fondo: el rate limit real es ~10 veces más estricto de lo asumido, y aparece un bug nuevo de pérdida silenciosa sin rastro

**Retomando el Bug 1 de la entrada anterior de hoy (429 en `Fetch Projobs`).** Antes de subir el `batchInterval` a ciegas, se verificó si el `retryOnFail` (`maxTries:3`, `waitBetweenTries:3000`) realmente se estaba disparando, dado que la ejecución real (`#20372`) no mostraba ningún tiempo extra atribuible a reintentos (81s para 135 items, exactamente `135 × 600ms`).

**Verificación del retry:** workflow temporal (creado y borrado en la misma sesión, sin tocar JazzHR) apuntando a `httpbin.org/status/429` con la config exacta de `Fetch Projobs`. Confirmado que el retry **sí se dispara** — un solo item tardó 26.4s reales antes de rendirse, muy por encima de un fallo instantáneo. Con 2 items y el mismo `batching` (`batchSize:1`, `batchInterval:600`), el nodo tardó ~10s combinados — evidencia de que el `batchInterval` solo espacia el **inicio** de cada request, no espera a que el item anterior termine su ciclo completo de reintentos antes de disparar el siguiente. Los reintentos corren de fondo, superpuestos con el disparo de los siguientes items — por eso no se veía tiempo extra en la ejecución real.

**Fix aplicado en caliente:** quitado `retryOnFail`/`maxTries`/`waitBetweenTries` de `Fetch Projobs` en el Poller real (`6gxbJ87rfAsbcCOO`), dejando `continueOnFail: true` intacto. Verificado que ningún otro nodo ni el `staticData` (`processedPairs`, 27 registros) se tocó con el `PUT`.

**Consecuencia real medida en la ejecución siguiente (`#20398`):** solo 8 pares válidos extraídos, con un 90% de fallo al primer intento (74 de 82 items con salida) — más alto que el "55%" documentado antes, porque ese 55% ya era la tasa **neta** después de que el retry rescatara casos en el 2º/3er intento. Sacar el retry no arregló nada de fondo, solo dejó de disimularlo, y de paso bajó el rendimiento del ciclo (no es pérdida permanente — `Extraer Pares` reintenta cada job/etapa en cada ciclo de 15 min, solo es más lento).

**Segunda hipótesis probada y descartada: que `Listar Jobs` (15 páginas, sin ningún `batching`) contaminara el presupuesto de rate limit compartido con `Fetch Projobs`.** Se le agregó el mismo `batching` que ya tenía `Fetch Projobs` (cambio también aplicado en caliente y verificado sin tocar otros nodos). Resultado en la ejecución siguiente (`#20415`): **exactamente los mismos 82 items de salida, los mismos 74 errores, y los mismos 8 candidatos exactos** que la ejecución anterior sin el cambio — cero efecto. Descarta la hipótesis del presupuesto compartido: JazzHR aparentemente rate-limitea por endpoint, no por cuenta global. El cambio queda aplicado en producción (inocuo, no se revirtió, no soluciona el problema real).

**Bug nuevo, no documentado antes de hoy: 52 items desaparecen de `Fetch Projobs` sin ningún rastro.** Comparando la entrada real de `Filtrar Open y Mapear Formatos` (134-135 items) contra la salida real de `Fetch Projobs`, en las 3 ejecuciones revisadas (`#20372`, `#20398`, `#20415`) faltan exactamente **52 items** que no aparecen ni como éxito ni como error — desaparecen antes de llegar a `Extraer Pares`, sin que `continueOnFail` deje ningún registro. Desglose real por ciclo: ~135 combinaciones → ~52 sin rastro → ~74-75 con error 429 visible → ~8 con éxito. Causa no investigada (¿n8n trunca los datos de ejecución grandes al guardarlos, o es un bug funcional real en la interacción `batching` + `continueOnFail`?) — queda pendiente.

**Medición directa del rate limit real, vía un segundo workflow temporal (creado y borrado en la sesión) contra el endpoint real `/job/{jobId}/projob` con la credencial real "JazzHR Cookie":** los headers de la respuesta no exponen `Retry-After` ni `X-RateLimit-*` (solo headers estándar de Cloudflare/JazzHR) — no hay atajo, hubo que medir empíricamente. Con 51 llamadas reales se observó un patrón de "balde que se rellena muy lento" (35 llamadas seguidas en 429, después 2 en 200, después vuelve a fallar): **~13 éxitos en ~143 segundos, es decir ≈1 solicitud exitosa cada ~10-11 segundos** — casi 20 veces más estricto que el `batchInterval` de 600ms asumido hasta hoy.

**Conclusión, cambia el diagnóstico de raíz del Bug 1:** con ~134 combinaciones job+etapa por ciclo y un ritmo sostenible real de ~10s por request, escanear todo el lote tomaría **~22 minutos** — más que la ventana de 15 minutos del cron. No es un problema de ajustar `batchInterval`, es un problema de diseño: el volumen de llamadas que necesita el Poller por ciclo excede lo que la cuenta de JazzHR permite en esa ventana, sin importar cuánto se espacien.

**Estado en producción al cerrar la sesión:**
- `Fetch Projobs`: sin `retryOnFail` (antes 3 intentos/3s) — cambio aplicado, no revertido.
- `Listar Jobs`: con `batching` (`batchSize:1`, `batchInterval:600`) agregado — cambio aplicado, confirmado sin efecto sobre el problema real, pero inocuo.
- Ambos verificados contra ejecuciones reales sin romper `staticData`/`processedPairs` ni otros nodos.

**Opciones evaluadas para el rediseño de fondo (ninguna implementada todavía):**
1. Rotar el escaneo entre ciclos — revisar una porción (~20) por ciclo en vez de las 134 combinaciones completas, completando el barrido total cada ~7 ciclos (~1h45). No depende de nada nuevo de la API. Recomendada por Claude.
2. Filtrar antes de llamar a `/projob` por algo más barato (ej. última modificación), si la API de JazzHR lo permite — no confirmado que exista.
3. Buscar un endpoint bulk que devuelva projobs de varios jobs en una sola llamada — no confirmado que exista.
4. Alargar el cron (ej. cada 25-30 min) y escanear todo a ~10-12s por llamada — más simple, pero degrada la latencia de detección de forma pareja para todos los candidatos.

**Pendiente para la próxima sesión:**
1. Decidir entre las 4 opciones de rediseño de arriba e implementarla.
2. Investigar la causa de los 52 items que desaparecen sin rastro en `Fetch Projobs`.
3. Bug 2 (`processedPairs` marcado antes de confirmar éxito), la visibilidad de fallas del Bug 1 (punto 2 original, sigue pendiente independientemente del rediseño), limpieza retroactiva de `processedPairs`, y William Velez Serrano — sin cambios, siguen pendientes de la entrada anterior de hoy.
4. Avisar a `eterron@fitspr.com` y `paolarod@fitspr.com` (correo con formato equivocado del 17 ago) — sigue bloqueado por token OAuth de Gmail vencido.
5. Decidir si revertir `saveDataSuccessExecution` a `"none"`.

**Lección:** cuando un mecanismo de mitigación (el retry) parece no estar funcionando por los tiempos observados, verificarlo de forma aislada antes de asumir que es decorativo — y al sacarlo, medir el impacto neto real (no solo si el síntoma original desaparece), porque puede estar compensando algo real aunque esté mal diseñado. Además: ante un rate limit sin headers informativos, medir empíricamente con volumen suficiente (no unas pocas llamadas espaciadas) antes de fijar cualquier parámetro de espaciado — el comportamiento de "balde lento" solo se ve con una ráfaga real.

### 18 Ago 2026 (continuación) — Escaneo rotativo implementado en producción: rate limit resuelto, y el "bug" de los 52 items sin rastro probablemente no era un bug

**Fix aplicado en `Convert Resume Poller` (`6gxbJ87rfAsbcCOO`) vía API de n8n (`PUT /workflows/{id}`, body mínimo con solo `name`/`nodes`/`connections`/`settings` para no arrastrar campos de solo lectura), verificado antes/después que solo cambiaron los 2 nodos esperados y que `staticData` (`processedPairs`, 28 registros, `lastRunStart`/`lastRunEnd`) quedó intacto:**

1. **`Filtrar Open y Mapear Formatos`** — en vez de devolver las ~134-135 combinaciones job+etapa completas, ahora trocea el listado en bloques de `CHUNK_SIZE = 20` usando un offset rotativo nuevo en `staticData.scanOffset` (indexado circular con `%`, así que da la vuelta sola al llegar al final). Barrido completo cada ~7 ciclos (~1h45), sin depender de ninguna feature nueva de la API de JazzHR.
2. **`Fetch Projobs`** — `batchInterval` subido de 600ms al ritmo sostenible real medido hoy (~10-11s), con margen a **12000ms**.

**Verificado con el primer ciclo real tras el deploy (ejecución `#20510`, 20:07:00–20:11:25 UTC, ~4m25s — el deploy quedó aplicado a las 19:59:36 UTC, después de la ejecución anterior `#20504`, así que `#20510` fue la primera en correr con el código nuevo):**
- `Filtrar Open y Mapear Formatos` entregó exactamente 20 items (antes ~134-135) y `staticData.scanOffset` avanzó de `0` a `20` — el troceo rotativo funciona.
- `Fetch Projobs`: **0 errores 429** en los 20 items (antes ~90% de fallo al primer intento) — el rate limit real (~1 request exitosa cada 10-11s) queda resuelto respetando ese ritmo.
- Duración total del ciclo (~4m25s) entra cómodo en la ventana de 15 min del cron.

**Reinterpretación del "bug nuevo" documentado en la entrada anterior de hoy (52 items sin rastro):** de los 20 items de este ciclo, solo 1 volvió con datos reales (`prospectId 404156667`, job `10920619`, step `10727655`); los otros 19 no aparecieron ni como éxito ni como error en la salida del nodo — mismo patrón. Pero con 0 errores 429 confirmados en este ciclo, la explicación ya no puede ser "429 silencioso" ni reintentos superpuestos. La causa más probable: la API de JazzHR devuelve un array JSON, y cuando un job no tiene ningún candidato en esa etapa puntual, devuelve `[]` — que n8n convierte automáticamente en **0 items de salida** para ese elemento del batch (ni error, ni item), no en un item vacío ni en un error visible. Es decir: la mayoría de los 134 combos job+etapa simplemente no tienen candidatos ahí en ese momento — comportamiento normal de la API, no pérdida de datos. Consistente con lo medido antes del fix (135 items → 82 con salida → 74 con error 429 + 8 candidatos reales + 53 "sin rastro"): con el rate limit real resuelto, es esperable que ese "sin rastro" siga apareciendo en proporción similar, ahora sin ningún error de por medio. **No confirmado al 100% con un solo ciclo** — falta ver que se sostenga en más ciclos y, si hace falta blindarlo, probar puntualmente un job/etapa conocido como vacío contra la API real.

**De paso, confirmado que `Dedup y Filtrar` funciona bien:** el único candidato real de este ciclo (`404156667-10920619-10727655`) ya estaba en `processedPairs` desde el 17 de agosto, así que quedó correctamente descartado (0 salida) — no es un reproceso.

**Estado en producción al cerrar esta entrada:**
- `Filtrar Open y Mapear Formatos`: trocea en bloques de 20 con offset rotativo (`scanOffset` en `staticData`) — cambio aplicado, verificado con un ciclo real.
- `Fetch Projobs`: `batchInterval` en 12000ms (antes 600ms) — cambio aplicado, verificado con un ciclo real (0 errores).
- Fuera de alcance de este cambio (siguen pendientes, sin tocar): Bug 2 (`processedPairs` marcado antes de confirmar éxito), visibilidad de fallas en `Extraer Pares`, limpieza retroactiva de `processedPairs`, William Velez Serrano.

**Pendiente para la próxima sesión:**
1. Monitorear 2-3 ciclos más del Poller para confirmar que el rate limit se sostiene en 0 errores y que la interpretación de "items sin rastro = resultado vacío legítimo" se mantiene consistente.
2. Bug 2, visibilidad de fallas, limpieza retroactiva de `processedPairs`, y William Velez Serrano — sin cambios, siguen pendientes.
3. Avisar a `eterron@fitspr.com` y `paolarod@fitspr.com` (correo con formato equivocado del 17 ago) — sigue bloqueado por token OAuth de Gmail vencido.
4. Decidir si revertir `saveDataSuccessExecution` a `"none"`.

**Lección:** antes de dar por buena la etiqueta "bug" de un síntoma (items que desaparecen sin rastro), separar primero las causas ya conocidas (en este caso, el rate limit real) — una vez resuelta la causa dominante, el mismo síntoma puede tener una explicación totalmente distinta y mucho más benigna (aquí, un array vacío de una API REST que n8n aplana a cero items). Diagnosticar de a una variable a la vez.

### 18 Ago 2026 (continuación) — Bug 2 corregido (`processedPairs` marcado antes de confirmar éxito), con un bug real adicional encontrado y corregido en el Processor durante la verificación

**Diseño acordado con Santiago antes de tocar código:** `NO_RESUME_FILE` se clasifica como falla permanente conocida (se marca, visible en el Sheet); `RENDER_FAILED` como transitoria (no se marca, reintenta); y se crea una pestaña nueva "Errores de Procesamiento" en el Sheet de log (`1EM7GeQ7AePoMyngzDsRgMgnjj85K_pIoqfa1u4ukAo4`, gid `1493500949`, columnas `candidateId/jobId/stepId/format/code/motivo/fecha`) — creada vía un workflow temporal con el método correcto (el `operation: "create"` del nodo Google Sheets no respeta el título pedido; hubo que renombrarla después con una llamada directa a `spreadsheets:batchUpdate` de la API real de Google Sheets, y cargar el encabezado con `values.update`).

**Fix aplicado en el Poller (`6gxbJ87rfAsbcCOO`):**
1. `Dedup y Filtrar` — ya no marca `processedPairs`, solo filtra y cachea antigüedad de 30 días.
2. `Disparar Convert Resume` — se activó `options.response.response.neverError` (verificado antes con un 401 real contra el propio Processor) para que las respuestas 4xx/5xx lleguen como item normal con el JSON real, no como `.error` genérico.
3. `Registrar Procesado` — reescrito: clasifica en 3 rutas (éxito → marca; `CORRUPT_FILE`/`GROUNDING_FAILED`/`NO_RESUME_FILE` → marca + visible; cualquier otra cosa incl. `RENDER_FAILED`, timeout, red, código desconocido → no marca, reintenta el próximo ciclo).
4. Nodo nuevo `¿Falla Permanente?` (IF) + `Registrar Error Permanente` (Google Sheets append a la pestaña nueva) insertados entre `Registrar Procesado` y `Esperar Entre Candidatos`.

**Bug real encontrado durante la verificación (no introducido hoy, pero destapado por el fix): el código específico de falla (`CORRUPT_FILE`, `GROUNDING_FAILED`) nunca llegaba al Poller.** Probando el fix contra un caso real conocido (Baxter Mickey Rains, `408862017`, `.doc` legacy → `CORRUPT_FILE`) vía un workflow temporal que llamaba directo al webhook real del Processor, la respuesta traía `code:"TRANSFORM_FAILED"` genérico, con el motivo real enterrado como string escapado dentro de `detail`. Causa: el nodo `POST /transform` del Processor no tenía `neverError`, así que cualquier 422 real de nuestro propio microservicio se envolvía en un `AxiosError` genérico (`$json.error.message`) en vez de traer el body real — exactamente el mismo problema que se acababa de corregir en el Poller, pero en el Processor. Corregido agregando `neverError` también a `POST /transform`.

**Segundo bug real, causado por el fix anterior y encontrado en el mismo ciclo de verificación:** al activar `neverError` en `POST /transform`, el nodo `¿Transform OK?` dejó de funcionar — su condición `$json.state !== 'failed'` asumía que el body de error tenía `state` en la raíz, pero FastAPI envuelve el `detail` de un `HTTPException` bajo una clave `detail` (`{"detail": {"state":"failed","code":"CORRUPT_FILE",...}}`), así que `$json.state` quedaba `undefined` y la condición pasaba como "OK" — el flujo seguía de largo hacia `POST /render`, que fallaba ahí con un error distinto y más engañoso (`RENDER_FAILED` en vez de `TRANSFORM_FAILED`/`CORRUPT_FILE`). Corregido: la condición ahora chequea `!$json.detail`, y `Responder Transform Fallido` lee `$json.detail.code`/`$json.detail.detail` (con fallback a `$json.error.message` para errores de red genuinos). Verificado de nuevo con Baxter Rains real: la respuesta ya trae `code:"CORRUPT_FILE"` limpio y la ejecución del Processor se detiene correctamente en `Responder Transform Fallido`, sin llegar a `Fetch Job`/SharePoint/render.

**Verificado en producción:**
- Ciclo real (ejecución `#20544`, primera con el código nuevo del Poller activo): candidato nuevo procesado con éxito, marcado correctamente, sin pasar por la rama de error permanente.
- El nodo `Registrar Error Permanente` se probó aislado con una fila de prueba (`TEST-999`) — funcionó al agregar el `schema` completo de columnas (un primer intento con `schema: []` falló con `columns.schema is required`); confirmado que el nodo real ya tenía el schema completo. Fila de prueba borrada de la pestaña real después de verificar.
- De paso se encontró y se borró un workflow temporal huérfano de una sesión anterior (15 ago, "TEMP - Leer Header Notificaciones Pendientes") que había quedado activo sin borrar — confirmado con Santiago antes de eliminarlo.

**Estado en producción al cerrar esta entrada:**
- Poller: `Dedup y Filtrar` sin marcado prematuro, `Disparar Convert Resume` con `neverError`, `Registrar Procesado` con clasificación de 3 rutas, `¿Falla Permanente?` + `Registrar Error Permanente` nuevos.
- Processor: `POST /transform` con `neverError`, `¿Transform OK?` y `Responder Transform Fallido` corregidos para leer el body real envuelto por FastAPI.
- Pestaña "Errores de Procesamiento" creada y con encabezados, vacía de datos reales (la fila de prueba se borró).
- Baxter Rains (`408862017-10936366-10727655`) ya estaba marcado en `processedPairs` desde antes del fix (por el comportamiento viejo de `Dedup y Filtrar` en la ejecución `#20537`) — no volverá a aparecer solo; si se quiere verlo pasar por el camino nuevo de punta a punta en un ciclo real, habría que desmarcarlo a mano o esperar otro candidato con falla permanente nueva.

**Pendiente para la próxima sesión:**
1. Confirmar con un ciclo real (no solo pruebas aisladas) que un candidato con falla permanente nueva termina marcado y con fila en "Errores de Procesamiento".
2. Auditar `POST /render` por el mismo patrón de `neverError` faltante (no confirmado si tiene el mismo problema de enmascarar el código real del fallo, pero es la misma clase de bug) — no tocado hoy, fuera de alcance.
3. Visibilidad de fallas en `Extraer Pares`, limpieza retroactiva de `processedPairs`, y William Velez Serrano — sin cambios, siguen pendientes.
4. Avisar a `eterron@fitspr.com` y `paolarod@fitspr.com` (correo con formato equivocado del 17 ago) — sigue bloqueado por token OAuth de Gmail vencido.
5. Decidir si revertir `saveDataSuccessExecution` a `"none"`.

**Lección:** activar `neverError` en un nodo HTTP puede destapar (o crear) bugs en la lógica de decisión que dependía de que n8n lanzara una excepción — cualquier IF/condición que hoy revisa `$json.error` para decidir éxito/fracaso hay que releerla asumiendo que, sin el error, el body de la respuesta puede tener una forma distinta a la esperada (en este caso, el wrapping de FastAPI bajo `detail`). No alcanza con arreglar el nodo HTTP: hay que seguir el rastro hasta el último nodo que consume su output.

### 18 Ago 2026 (continuación) — Bug real de candidatos duplicados encontrado y corregido: reproceso manual no marcaba `processedPairs`

**Santiago reportó el problema real: Emilio Nieves ya había sido procesado y volvió a procesarse.** Confirmado con el Sheet real (captura de pantalla): fila 30 (2026-08-18 8:50, `resultado: OK`, correo real enviado — el reproceso manual de esta mañana, ver entrada anterior de hoy) y fila 37 (2026-08-18 17:12, `resultado: OK_ENCOLADO`) para el mismo candidato (`431957584`) y mismo job (`10936366`).

**Causa raíz:** el reproceso manual de esta mañana llamó directo al webhook interno del Processor (`jazzhr-convert-resume`), sin pasar por `Dedup y Filtrar` del Poller — así que `staticData.processedPairs` nunca se enteró de que ese candidato ya estaba hecho. Cuando el ciclo automático de las 21:07 UTC lo volvió a escanear (su job/etapa cayó en el bloque rotado de ese ciclo), no estaba marcado y lo reprocesó de cero: segundo `.docx`, segunda subida a SharePoint, y un correo duplicado encolado (fuera de horario laboral, `OK_ENCOLADO`) que habría salido solo en el próximo ciclo del `Notification Queue Flusher`.

**Mitigación inmediata:** se sacó la fila duplicada de Emilio Nieves de la pestaña "Notificaciones Pendientes" (vía workflow temporal, `deleteDimension` directo a la API de Sheets) antes de que el Flusher la despachara — verificado que solo quedó la fila legítima de Orlando Yaniel Casanova Torres en la cola.

**Fix de fondo, con Santiago confirmando ir por la opción robusta (chequear el Sheet real, no solo el `staticData` local):** nuevo nodo `Leer Log Real` (HTTP directo a `spreadsheets.values.get` de la API de Sheets, columnas B:H del log principal) insertado entre `Extraer Pares` y `Dedup y Filtrar`. `Dedup y Filtrar` reescrito para leer `$('Extraer Pares').all()` explícitamente (antes usaba `$input.all()`, ya no aplica porque su input directo ahora es `Leer Log Real`) y filtrar contra dos fuentes: `staticData.processedPairs` (como antes) **y** un `Set` construido a partir de las filas reales del Sheet (`candidateId-jobId-format`) — así cualquier candidato ya entregado por *cualquier* camino (automático o reproceso manual) queda cubierto, sin depender de que alguien recuerde marcarlo a mano.

**Bug real encontrado en el propio fix, antes de darlo por bueno:** la columna `format` del Sheet guarda la etiqueta legible que arma `Calcular Resultado Final` (`datos.formatLabel`, ej. `"Non Template"`), no el código interno que usa el Poller (`non_template`) — el primer chequeo contra el Sheet nunca iba a matchear nada por ese desface de formato. Corregido normalizando la etiqueta del Sheet (`.toLowerCase().replace(/\s+/g, '_')`) antes de comparar — verificado que los 3 formatos reales (`"Non Template"`→`non_template`, `"New Format"`→`new_format`, `"BD Format"`→`bd_format`) normalizan exactamente al código interno.

**Verificado con un caso real vía workflow temporal aislado (sin tocar producción ni disparar un reproceso real):** Yajaira Ortiz Ruiz (`422900145`, job `10957423`) — procesada manualmente esta mañana, nunca marcada en `processedPairs` (mismo patrón que Emilio Nieves, todavía sin corregir cuando se hizo esta prueba). El chequeo nuevo la detectó correctamente como ya entregada vía el Sheet real (`enSheet: true`) aunque no estuviera en el `staticData` local (`enLocal: false`) — confirma que el fix habría evitado también su duplicado si su job/etapa hubiera caído en un ciclo automático futuro.

**De paso, limpieza de un workflow temporal huérfano de una sesión anterior** (`TEMP - Leer Header Notificaciones Pendientes`, creado el 15 ago, sin relación con lo de hoy) que había quedado activo — confirmado con Santiago antes de borrarlo.

**Estado en producción al cerrar esta entrada:**
- Poller: nodo nuevo `Leer Log Real` + `Dedup y Filtrar` reescrito para chequear contra el Sheet real (normalizado) además del `staticData` local.
- Cola de notificaciones limpia (sin el duplicado de Emilio Nieves).
- Yajaira Ortiz Ruiz y Emilio Nieves siguen sin estar en `processedPairs` (Yajaira nunca se marcó; Emilio sí quedó marcado, pero por el reproceso automático duplicado, no por el fix) — no van a volver a duplicarse gracias al chequeo nuevo contra el Sheet real, aunque el `staticData` local siga "desactualizado" para esos dos casos puntuales. No hace falta corregir eso retroactivamente: el chequeo contra el Sheet ya los cubre hacia adelante.

**Pendiente para la próxima sesión:**
1. Confirmar en un ciclo real (no solo la prueba aislada de hoy) que el chequeo contra el Sheet real sigue funcionando cuando efectivamente hay un candidato ya entregado en el bloque rotado del ciclo.
2. Bug 1 (confirmar más ciclos sin 429), auditar `POST /render` por el mismo patrón de `neverError` faltante, visibilidad de fallas en `Extraer Pares`, limpieza retroactiva de `processedPairs`, y William Velez Serrano — sin cambios, siguen pendientes.
3. Avisar a `eterron@fitspr.com` y `paolarod@fitspr.com` (correo con formato equivocado del 17 ago) — sigue bloqueado por token OAuth de Gmail vencido.
4. Decidir si revertir `saveDataSuccessExecution` a `"none"`.

**Lección:** cuando existen dos caminos para lograr el mismo resultado de negocio (el ciclo automático del Poller y el reproceso manual vía webhook interno), cualquier mecanismo de "ya hecho" que viva solo en el camino automático (`staticData` del Poller) va a quedar desincronizado apenas alguien use el otro camino — la fuente de verdad tiene que ser algo que ambos caminos actualicen, no un estado local de uno de los dos. Y al construir un chequeo contra datos reales de un Sheet, no asumir que los nombres de campo coinciden entre lo que un nodo escribe (`formatLabel`) y lo que otro nodo espera leer (`format` interno) — verificar con datos reales antes de confiar en el fix.

### 19 Ago 2026 — Crashes por OOM diagnosticados y corregidos (`saveDataSuccessExecution`), gap de ~6h45m sin ejecuciones del Poller, y retry agregado a `Leer Log Real`

**Investigación de crashes reportados desde la mañana:** revisando el historial real de ejecuciones del `Convert Resume Poller` (`6gxbJ87rfAsbcCOO`) vía API de n8n, se confirmó un patrón de crashes (`status: crashed`, sin `lastNodeExecuted`, sin datos de error capturados) coincidiendo con `saveDataSuccessExecution: "all"` en la configuración del workflow — cada ejecución exitosa estaba guardando el JSON completo de datos (~49.5 MB por ejecución), agotando la memoria del proceso. Confirmado con archivos temporales de ejecuciones reales en `/tmp` antes de descartarlos. **Fix aplicado (~10:03am hora local / 15:03 UTC):** `saveDataSuccessExecution` revertido a `"none"` (el valor que ya usan el resto de los workflows de la instancia), vía `PUT /workflows/{id}` con body mínimo, verificado que `active` y `staticData` (`processedPairs`, `scanOffset`) quedaron intactos.

**Hallazgo nuevo en esta sesión: gap real de ~6h45m sin ninguna ejecución del Poller (14:52 UTC → 21:37 UTC de hoy).** El cron dispara cada 15 min (`7,22,37,52 * * * *`), pero no hay ninguna ejecución registrada en esa ventana — mientras que otros workflows de la misma instancia (ej. `A2fusycYaExIdZ4R`) siguieron corriendo cada 15 min sin interrupción en el mismo lapso, descartando una caída de la instancia completa. El `updatedAt` del workflow (`2026-08-19T15:05:08Z`) cae justo al inicio del gap, coincidiendo con el momento del fix de `saveDataSuccessExecution` — la hipótesis más probable es que la actualización vía API dejó el trigger de cron sin re-disparar hasta 6h45m después, pero no se confirmó la causa exacta (no se revisó log de instancia a ese nivel). **No hubo pérdida de candidatos:** el escaneo rotativo con offset y el dedup contra el Sheet real hacen que lo no escaneado en ese lapso simplemente se cubra en un ciclo posterior.

**La primera ejecución tras el gap (`#20906`, 21:37–21:41 UTC) falló** con `Service unavailable` (503) en el nodo `Leer Log Real` (la llamada HTTP directa a `spreadsheets.values.get` de Google Sheets, agregada el 18 ago para el dedup contra el log real) — el mismo error exacto ya había tirado la ejecución `#20642` (04:07 UTC, antes del fix de OOM). Confirmado con las alertas reales por correo (`fitsscreening@gmail.com`, asunto `FITS AI Screening — Error real en "JazzHR - Convert Resume Poller"`): 2 de las 4 alertas de las últimas 24h corresponden a este mismo error (18 ago 23:11 y hoy 16:41 hora local) — un patrón recurrente, no un caso aislado, y el nodo no tenía ningún retry configurado.

**Fix aplicado:** `retryOnFail: true`, `maxTries: 3`, `waitBetweenTries: 5000` (3 intentos, 5s entre cada uno) agregado al nodo `Leer Log Real` vía `PUT /workflows/{id}` (mismo método de body mínimo usado en fixes anteriores). Verificado tras el update: `active` sigue `true` y `staticData` (`processedPairs` con 35 registros, `scanOffset: 63`) quedó intacto.

**Estado en producción al cerrar esta entrada:**
- `saveDataSuccessExecution: "none"` — aplicado y verificado, sin crashes por OOM confirmados desde entonces (solo 1 ejecución real desde el fix hasta ahora, muestra insuficiente para darlo por sostenido).
- `Leer Log Real` con retry (3 intentos, 5s) — aplicado, no verificado aún contra un 503 real (el error es intermitente, no reproducible a demanda).
- Gap de 6h45m del Poller — causa no confirmada al 100%, sin cambios adicionales aplicados más allá del retry.

**Pendiente para la próxima sesión:**
1. Confirmar con Santiago si desactivó/reactivó el Poller manualmente durante la investigación de esta mañana, para descartar o confirmar la hipótesis del gap.
2. Monitorear varios ciclos más para confirmar que no vuelven los crashes por OOM ni el gap de horas sin ejecución.
3. Confirmar que el retry de `Leer Log Real` efectivamente evita que un 503 tire la ejecución completa (esperar a que se repita el error).
4. Bug 1 (confirmar más ciclos sin 429), auditar `POST /render` por el mismo patrón de `neverError` faltante, visibilidad de fallas en `Extraer Pares`, limpieza retroactiva de `processedPairs`, y William Velez Serrano — sin cambios, siguen pendientes.
5. Avisar a `eterron@fitspr.com` y `paolarod@fitspr.com` (correo con formato equivocado del 17 ago) — sigue bloqueado por token OAuth de Gmail vencido.

**Lección:** una actualización de configuración aparentemente inocua (cambiar `saveDataSuccessExecution`) puede tener un efecto colateral no documentado sobre el trigger de cron — vale la pena, tras cualquier `PUT` a un workflow activo, confirmar no solo que el campo cambiado quedó bien sino que las ejecuciones programadas siguen disparando en el intervalo esperado, no solo que el workflow sigue en `active: true`.

### 20 Ago 2026 — Falsa alarma de "17 horas sin ejecutar" explicada (alerta de crash con retraso real), y bug real de selección de CV encontrado y corregido con un candidato real (Willard R. Marrero Ginorio)

**Santiago reportó un correo de `fitsscreening@gmail.com` sobre un crash del Poller "hace 19 minutos".** Investigación inicial vía API de n8n encontró que la última ejecución del `Convert Resume Poller` visible en el historial era un `crashed` (OOM en `Fetch Projobs`) de casi 20 horas antes, sin ninguna ejecución registrada desde entonces — conclusión preliminar equivocada: "el Poller lleva 17 horas sin correr". **Santiago señaló correctamente que eso no podía ser cierto** porque el Sheet mostraba 2 candidatos procesados esa misma tarde, con correo real enviado a los reclutadores y `.docx` real en SharePoint.

**Causa real, confirmada con datos de n8n:** desde el fix de OOM del 19 ago, el Poller corre con `saveDataSuccessExecution: "none"` — las ejecuciones **exitosas** no dejan ningún registro en el historial, solo las que terminan en `error`/`crashed`. Los 2 candidatos del Sheet correspondían a ejecuciones reales y correctas del Processor (con el token interno correcto, que solo el Poller tiene), disparadas por ciclos de cron normales que simplemente no aparecen en el historial por diseño — no por ausencia de ejecución. El error de razonamiento fue interpretar "sin registro" como "sin ejecución".

**Segundo hallazgo, real y nuevo:** comparando el disparo de la alerta de error contra otro caso reciente con estado `error` (no `crashed`), se confirmó que **el workflow de alerta (`JazzHR - Error Alert Global`) puede tardar hasta ~17 horas en dispararse cuando el Poller termina en estado `crashed`** (proceso matado por OOM), mientras que un estado `error` normal dispara la alerta en ~1-2 segundos. n8n solo pudo confirmar el crash y ejecutar el workflow de error mucho después del incidente real, probablemente en un ciclo de recuperación interno de la instancia — no se investigó el mecanismo exacto porque es interno de n8n Cloud, sin control desde la API pública. **Esto hace que la alerta de crash no sea confiable para detección a tiempo de este tipo específico de falla** (aunque el Poller sí sigue funcionando con normalidad después de un crash aislado, así que no representa una caída real del servicio).

**Bug real de selección de CV encontrado con un candidato real.** Santiago compartió el CV real de Willard R. Marrero Ginorio (`resume_6a7885bfd4f2c.pdf`, subido a JazzHR el 9 ago 2026) contra el `.docx` que el sistema generó para él en formato Non Template — el renderizado tenía la sección "PROFESSIONAL EXPERIENCE" vacía (perdiendo los 7 puestos reales en Baxter Healthcare, Collins Aerospace, etc.) y un listado de Skills completamente ajeno al candidato (SolidWorks, PTC Creo, ANSYS, Matlab, SAP, torno/fresadora — ninguno de esos términos aparece en su CV real).

**Causa raíz:** el nodo `Encontrar CV` del Processor (`mp3U5XDSxLCAX9kI`) prioriza documentos marcados `isResume: true` por JazzHR por encima de la fecha:
```js
let ranked = [...markedAsResume, ...nameMatches];  // markedAsResume siempre primero, sin mirar fecha
const doc = toDoc(ranked[0]);
```
Willard lleva aplicando desde 2020, así que su perfil acumula **24 documentos `resume_*.pdf`** de 5+ años. De esos 24, JazzHR solo tiene marcado `isResume: true` en uno: `Willard_R._Marrero_Ginorio_Resum.pdf`, creado el **24 feb 2021** (108 KB, CV de recién graduado sin experiencia profesional aún, con skills genéricos de estudiante — coincide exactamente con lo que salió mal renderizado). El CV correcto y actual (`resume_6a7885bfd4f2c.pdf`, 9 ago 2026, 21 KB) tiene `isResume: false` como todos los demás, así que nunca se elegía. JazzHR, aparentemente, no reasigna ese flag al resume más reciente cuando un candidato reaplica después de años.

**Fix aplicado:** se combinan los candidatos (`markedAsResume` + `nameMatches`) y se ordenan por `createdAt` descendente antes de elegir el primero, en vez de confiar en el flag `isResume` sin mirar la fecha. **Verificado antes de tocar producción:** se replicó la lógica vieja y nueva en Node.js (mismo motor de n8n) contra 7 casos — los 2 reales (Willard, y Migdalia Cruz Leon con un solo documento) más 5 sintéticos (isResume=true que también es el más reciente, empate por nombre resuelto por fecha, sin documentos con/sin `resumeId` de fallback, ningún match cae al más reciente de todos). Los 7 casos dieron el resultado esperado, incluyendo que los casos sin ambigüedad (Migdalia, y el sintético con isResume=true+más reciente) se comportan idéntico a la lógica vieja — sin regresión. Aplicado vía `PUT /workflows/{id}` y verificado con diff completo del workflow antes/después: mismos 35 nodos, mismas conexiones, mismos settings — solo cambió el código de `Encontrar CV`.

**Reproceso real de Willard tras el fix:** primer intento bloqueado por `grounding.py` (`GROUNDING_NAME_NOT_FOUND`, el nombre salió como "Willillard Marreroro" — una alucinación puntual del modelo en esa llamada, confirmada no-determinística corriendo `agent.transform()` en local contra el mismo texto extraído, que en ese segundo intento produjo `"Willard R. Marrero Ginorio"` correctamente). El chequeo de grounding hizo exactamente lo que debía: bloquear antes de generar/enviar nada malo. Segundo intento vía webhook (`POST /webhook/jazzhr-convert-resume`, mismo token interno visible en el nodo `Verificar Token Interno`) exitoso: `.docx` correcto regenerado y subido a SharePoint (sobrescribiendo el archivo incorrecto), correo encolado (`OK_ENCOLADO`, fuera de horario laboral) para el próximo ciclo del `Notification Queue Flusher` — sin riesgo de duplicado porque el primer intento nunca llegó a `/render` ni a la notificación.

**Pendiente:**
1. Avisar al reclutador de Willard que el primer CV que recibió estaba mal (el sistema ya le va a mandar el correcto solo, pero amerita una aclaración humana dado que ya recibió uno antes).
2. Evaluar si otros candidatos recurrentes de años anteriores ya recibieron un CV desactualizado antes de este fix — no se auditó el histórico completo, solo se corrigió el caso puntual de Willard.
3. Bug 1 (confirmar más ciclos sin 429), auditar `POST /render` por el mismo patrón de `neverError` faltante, visibilidad de fallas en `Extraer Pares`, limpieza retroactiva de `processedPairs`, y William Velez Serrano — sin cambios, siguen pendientes.
4. Avisar a `eterron@fitspr.com` y `paolarod@fitspr.com` (correo con formato equivocado del 17 ago) — sigue bloqueado por token OAuth de Gmail vencido.

**Lección:** "no aparece en el historial de ejecuciones" no es lo mismo que "no se ejecutó" — hay que revisar la configuración de `saveDataSuccessExecution` del workflow antes de concluir que algo dejó de correr, y cruzar con evidencia independiente (el Sheet real, en este caso) antes de recomendar una acción sobre producción. Y en integraciones con sistemas externos (JazzHR) que traen sus propios flags de "verdad" (`isResume`), no asumir que ese flag se mantiene actualizado con el tiempo — verificar contra la fecha real cuando hay más de un candidato posible, especialmente con candidatos que reaplican después de años.

### 24 Ago 2026 — Cambio de alcance pedido por FITS: el correo va ahora al recruiter que movió al candidato a Convert Resume, no al hiring lead del job

**Pedido del cliente:** que el correo de notificación llegue al reclutador que específicamente movió a *ese* candidato a la etapa Convert Resume, no al `job.hiringLeadAccountId` (el recruiter asignado al puesto completo, el mismo para todos los candidatos de ese job sin importar quién los mueva).

**Investigación del endpoint real, hecha con Santiago vía DevTools del navegador (no había forma de adivinarlo por API a ciegas):** JazzHR no tiene webhooks (restricción ya conocida) y la API interna `api.jazz.co` no tiene un endpoint obvio de "historial de cambios de etapa" — se probaron y descartaron por 404 `/prospect/{id}/statusHistory`, `/projob/{id}/statusHistory`, `/prospect/{id}/timeline`, `/activity?prospectId=`, y `/prospect/{id}/activity` sin parámetros (este último responde, pero solo devuelve la última nota/comentario, no el historial de cambios de etapa). El endpoint real, capturado por Santiago abriendo la pestaña Discussion de un candidato real (Brian Bonet) con Network abierto en DevTools:

```
GET https://api.jazz.co/prospect/{candidateId}/activity?_partialAcl=true&projobIds[]={projobId}&expand=user,projob,interview,transientUser,toWorkflowStep,createdBy,questionnaire
```

Devuelve un array con todo el historial de actividad del `projob` (cambios de etapa, respuestas de entrevista, merges). El evento relevante tiene `activityType: "workflow-step-change"`, `toWorkflowStepId` (matchea el `step_id` ya mapeado en `conocimiento/jazzhr-stepids-convert-resume.md`) y `createdBy` con el nombre/email real de quien hizo el movimiento.

**Se descartó por el camino el campo `recruiter`/`recruiterId` del candidato** (`api.jazz.co/prospect/{id}?expand=recruiter`) como alternativa — vino `null` en 3 de 4 candidatos reales probados (Steven Palmer-Velazquez, Yajaira Ortiz Ruiz, Ruth Sotomayor Clavell), solo poblado en uno (Aida Cruz Santoni). Es un campo manual/opcional de "recruiter asignado al candidato", no un registro confiable de quién ejecutó el movimiento de etapa.

**Cambio aplicado al Processor** (`mp3U5XDSxLCAX9kI`): nodo nuevo `Fetch Activity` insertado entre `Fetch Hiring Lead` y `Calcular Datos Entrega` (misma credencial `JazzHR Cookie`, `onError: continueRegularOutput` para que un fallo de esta llamada nunca tumbe el pipeline). `Calcular Datos Entrega` ahora resuelve `recruiterEmail` en cascada: `createdBy.email` del evento `workflow-step-change` más reciente hacia el `stepId` de Convert Resume → si no hay match, `job.hiringLeadAccountId` (comportamiento anterior, sin cambios) → si tampoco, `reclutamiento@fitspr.com` (fallback ya existente). El projobId correcto se resuelve buscando en `Obtener Candidato().projobs` (ya trae `expand=projobs` desde antes) el que coincide con el `jobId` del payload.

**Verificado sin tocar el pipeline real** (para no duplicar procesamiento ni disparar correos de prueba a reclutadores reales) con un workflow temporal de solo lectura que replica exactamente la lógica nueva, contra 3 candidatos reales ya procesados:
- **Brian Bonet** (candidateId 340972608, job "Engineering Technician" 10744550): `moverEmail = paola@fitspr.com`, coincide con el `hiringLeadAccountId` del job (misma persona) — no distingue comportamiento, pero confirma que el mecanismo funciona de punta a punta.
- **Yajaira Ortiz Ruiz** (candidateId 422900145, job "Process Development Scientist -04" 10957423): **caso real donde el comportamiento cambia.** El correo que salió en producción el 18 ago (lógica vieja) fue a `jsoto@fitspr.com` (hiring lead del job). El evento real de `workflow-step-change` muestra que quien la movió a Convert Resume fue **Paola Rodríguez López** (`paolarod@fitspr.com`) — con el fix, el correo habría llegado a la persona correcta.
- Ruth Sotomayor Clavell usada de control adicional (mismo patrón, sin discrepancia).

**Pendiente:** validar con un candidato nuevo procesado de punta a punta por el ciclo automático del Poller (no se forzó un reproceso real para no duplicar correos a candidatos ya notificados) y confirmar que el correo real llega al recruiter correcto cuando el mover y el hiring lead son distintos.
